# راهنمای کامل فرآیند شیفت‌بندی از صفر تا صد

این راهنما به صورت مرحله به مرحله تمام مراحل لازم برای انجام عملیات شیفت‌بندی را از ابتدا تا انتها توضیح می‌دهد.

---

## 📋 فهرست مطالب

1. [پیش‌نیازها](#پیش-نیازها)
2. [مرحله 1: ایجاد تقویم (ShiftDate)](#مرحله-1-ایجاد-تقویم-shiftdate)
3. [مرحله 2: ایجاد بیمارستان](#مرحله-2-ایجاد-بیمارستان)
4. [مرحله 3: ایجاد دپارتمان](#مرحله-3-ایجاد-دپارتمان)
5. [مرحله 4: تعریف تخصص‌ها (Specialty)](#مرحله-4-تعریف-تخصصها-specialty)
6. [مرحله 5: تعریف شیفت‌ها (Shift)](#مرحله-5-تعریف-شیفتها-shift)
7. [مرحله 6: ایجاد کاربران و پرسنل](#مرحله-6-ایجاد-کاربران-و-پرسنل)
8. [مرحله 7: تنظیمات شیفت‌بندی دپارتمان](#مرحله-7-تنظیمات-شیفت-بندی-دپارتمان)
9. [مرحله 8: تنظیمات الگوریتم‌های بهینه‌سازی (اختیاری)](#مرحله-8-تنظیمات-الگوریتمهای-بهینه-سازی-اختیاری)
10. [مرحله 8.۵: سهمیه شب ماهانه](#مرحله-85-سهمیه-شب-ماهانه)
11. [مرحله 8.۶: سهمیه صبح/عصر ماهانه](#مرحله-86-سهمیه-صبحعصر-ماهانه)
12. [مرحله 8.۷: سهمیه ترکیبی صبح/عصر و صبح/شب](#مرحله-87-سهمیه-ترکیبی-صبحعصر-و-صبحشب)
13. [مرحله 9: ثبت درخواست‌های شیفت توسط کارکنان](#مرحله-9-ثبت-درخواستهای-شیفت-توسط-کارکنان)
14. [مرحله 10: بررسی و تأیید درخواست‌های شیفت](#مرحله-10-بررسی-و-تأیید-درخواستهای-شیفت)
15. [مرحله 11: اجرای عملیات شیفت‌بندی](#مرحله-11-اجرای-عملیات-شیفت-بندی)
16. [نکات مهم و خطاهای رایج](#نکات-مهم-و-خطاهای-رایج)

---

## پیش‌نیازها

قبل از شروع، مطمئن شوید که:

- ✅ SQL Server در حال اجرا است
- ✅ مهاجرت‌های پایگاه داده (EF Migrations) اجرا شده‌اند
- ✅ API در حال اجرا است
- ✅ یک کاربر با نقش **Admin** یا **Supervisor** برای انجام عملیات مدیریتی دارید
- ✅ توکن احراز هویت (JWT Token) برای دسترسی به API دارید

**نکته مهم:** تمام تاریخ‌های ورودی در API باید به صورت شمسی و در قالب `yyyy/MM/dd` (مثلاً `1404/01/15`) باشند. سیستم به صورت خودکار آن‌ها را به میلادی تبدیل می‌کند.

---

## مرحله 1: ایجاد تقویم (ShiftDate)

**هدف:** ایجاد تقویم شمسی سال مورد نظر برای نگاشت تاریخ‌ها و تعطیلات

### اکشن 1.1: ایجاد تقویم سال

**Endpoint:** `POST /api/CalendarSeeder/SeedYear?year={سال_شمسی}`

**مثال:**
```http
POST /api/CalendarSeeder/SeedYear?year=1404
```

**توضیحات:**
- قبل از اجرای اکشن فوق بایستی فایل json مربوط به تعطیلات سال موردنظر را به نام holidays_سال موردنظر.json ساخته و در مسیر ..\ShiftYar\ShiftYar.Api\Resources\Holidays قرار دهیم.
- این اکشن تمام روزهای سال شمسی را در پایگاه داده ایجاد می‌کند
- برای هر روز، تاریخ میلادی، تاریخ شمسی، نام روز و تعطیل بودن یا نبودن ثبت می‌شود
- این مرحله باید قبل از هر عملیات دیگری انجام شود

**خروجی موفق:**
```json
{
  "message": "Calendar for 1404 seeded successfully."
}
```

### اکشن 1.2: بررسی تقویم ایجاد شده

**Endpoint:** `GET /api/CalendarSeeder?PersianDateStart={تاریخ_شروع}&PersianDateEnd={تاریخ_پایان}`

**مثال:**
```http
GET /api/CalendarSeeder?PersianDateStart=1404/01/01&PersianDateEnd=1404/01/10
```

**توضیحات:**
- برای اطمینان از ایجاد صحیح تقویم، می‌توانید تاریخ‌های ایجاد شده را بررسی کنید

---

## مرحله 2: ایجاد بیمارستان

**هدف:** ایجاد بیمارستان به عنوان بالاترین سطح سازمانی

### اکشن 2.1: ایجاد بیمارستان جدید

**Endpoint:** `POST /api/Hospital`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "siamCode": "12345",
  "name": "بیمارستان امام خمینی",
  "province": "تهران",
  "city": "تهران",
  "address": "خیابان ولیعصر، پلاک 123",
  "phoneNumbers": [
    {
      "phoneNumber": "021-12345678"
    }
  ],
  "email": "info@hospital.com",
  "website": "https://hospital.com",
  "description": "بیمارستان مرجع",
  "isActive": true
}
```

**توضیحات:**
- `siamCode`: کد سیام بیمارستان (کد شناسایی یکتا)
- `name`: نام بیمارستان
- `province` و `city`: استان و شهر
- `phoneNumbers`: آرایه‌ای از شماره تلفن‌ها
- `isActive`: وضعیت فعال/غیرفعال بودن بیمارستان

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "بیمارستان با موفقیت ایجاد شد.",
  "data": {
    "id": 1,
    "siamCode": "12345",
    "name": "بیمارستان امام خمینی",
    ...
  }
}
```

### اکشن 2.2: دریافت لیست بیمارستان‌ها

**Endpoint:** `GET /api/Hospital`

**مثال:**
```http
GET /api/Hospital?PageNumber=1&PageSize=10
```

---

## مرحله 3: ایجاد دپارتمان

**هدف:** ایجاد دپارتمان‌های مختلف بیمارستان

### اکشن 3.1: ایجاد دپارتمان جدید

**Endpoint:** `POST /api/Department`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "name": "دپارتمان قلب و عروق",
  "description": "بخش قلب و عروق بیمارستان",
  "hospitalId": 1,
  "supervisorId": null,
  "isNightLover": false,
  "isActive": true
}
```

**توضیحات:**
- `name`: نام دپارتمان
- `hospitalId`: شناسه بیمارستان (از مرحله 2)
- `supervisorId`: شناسه کاربری که به عنوان سوپروایزر تعیین می‌شود (می‌تواند null باشد و بعداً تنظیم شود)
- `isNightLover`: مشخص می‌کند که آیا این دپارتمان "شب‌دوست" است یا "شب‌گریز" (برای تقسیم عادلانه‌تر شیفت‌های شب)
- `isActive`: وضعیت فعال/غیرفعال

**نکته:** اگر هنوز سوپروایزر ایجاد نشده، می‌توانید `supervisorId` را `null` بگذارید و بعداً آن را به‌روزرسانی کنید.

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "دپارتمان با موفقیت ایجاد شد.",
  "data": {
    "id": 1,
    "name": "دپارتمان قلب و عروق",
    "hospitalId": 1,
    ...
  }
}
```

### اکشن 3.2: دریافت لیست دپارتمان‌ها

**Endpoint:** `GET /api/Department`

**مثال:**
```http
GET /api/Department?HospitalId=1&PageNumber=1&PageSize=10
```

### اکشن 3.3: به‌روزرسانی دپارتمان (برای تنظیم سوپروایزر)

**Endpoint:** `PUT /api/Department/{id}`

**Body نمونه:**
```json
{
  "name": "دپارتمان قلب و عروق",
  "description": "بخش قلب و عروق بیمارستان",
  "hospitalId": 1,
  "supervisorId": 5,
  "isNightLover": false,
  "isActive": true
}
```

---

## مرحله 4: تعریف تخصص‌ها (Specialty)

**هدف:** تعریف تخصص‌های مورد نیاز برای هر دپارتمان

### اکشن 4.1: ایجاد تخصص جدید

**Endpoint:** `POST /api/Specialty`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "departmentId": 1,
  "specialtyNameId": 1,
  "isActive": true
}
```

**توضیحات:**
- `departmentId`: شناسه دپارتمان
- `specialtyNameId`: شناسه نام تخصص (ابتدا باید نام تخصص‌ها را ایجاد کنید)
- `isActive`: وضعیت فعال/غیرفعال

**نکته:** ممکن است ابتدا نیاز به ایجاد `SpecialtyName` داشته باشید. برای این کار از `POST /api/SpecialtyName` استفاده کنید.

**مثال ایجاد نام تخصص:**
```json
{
  "name": "پرستار"
}
```

### اکشن 4.2: دریافت لیست تخصص‌ها

**Endpoint:** `GET /api/Specialty`

**مثال:**
```http
GET /api/Specialty?DepartmentId=1&PageNumber=1&PageSize=10
```

---

## مرحله 5: تعریف شیفت‌ها (Shift)

**هدف:** تعریف شیفت‌های کاری برای هر دپارتمان (صبح، عصر، شب)

### اکشن 5.1: ایجاد شیفت جدید

**Endpoint:** `POST /api/Shift`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه برای شیفت صبح:**
```json
{
  "departmentId": 1,
  "label": 0,
  "startTime": "07:00:00",
  "endTime": "15:00:00",
  "requiredSpecialties": [
    {
      "specialtyId": 1,
      "requiredMaleCount": 2,
      "requiredFemaleCount": 2,
      "requiredTottalCount": 4
    },
    {
      "specialtyId": 2,
      "requiredMaleCount": 1,
      "requiredFemaleCount": 1,
      "requiredTottalCount": 2
    }
  ]
}
```

**توضیحات:**
- `departmentId`: شناسه دپارتمان
- `label`: نوع شیفت
  - `0` = صبح (Morning)
  - `1` = عصر (Evening)
  - `2` = شب (Night)
- `startTime`: ساعت شروع شیفت (فرمت `HH:mm:ss`)
- `endTime`: ساعت پایان شیفت (فرمت `HH:mm:ss`)
- `requiredSpecialties`: آرایه‌ای از تخصص‌های مورد نیاز برای این شیفت
  - `specialtyId`: شناسه تخصص
  - `requiredMaleCount`: تعداد مورد نیاز آقایان
  - `requiredFemaleCount`: تعداد مورد نیاز خانم‌ها
  - `requiredTottalCount`: تعداد کل مورد نیاز

**مثال برای شیفت عصر:**
```json
{
  "departmentId": 1,
  "label": 1,
  "startTime": "15:00:00",
  "endTime": "23:00:00",
  "requiredSpecialties": [
    {
      "specialtyId": 1,
      "requiredMaleCount": 2,
      "requiredFemaleCount": 2,
      "requiredTottalCount": 4
    }
  ]
}
```

**مثال برای شیفت شب:**
```json
{
  "departmentId": 1,
  "label": 2,
  "startTime": "23:00:00",
  "endTime": "07:00:00",
  "requiredSpecialties": [
    {
      "specialtyId": 1,
      "requiredMaleCount": 1,
      "requiredFemaleCount": 1,
      "requiredTottalCount": 2
    }
  ]
}
```

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "شیفت با موفقیت ایجاد شد.",
  "data": {
    "id": 1,
    "departmentId": 1,
    "label": 0,
    "startTime": "07:00:00",
    "endTime": "15:00:00",
    ...
  }
}
```

### اکشن 5.2: دریافت لیست شیفت‌ها

**Endpoint:** `GET /api/Shift`

**مثال:**
```http
GET /api/Shift?DepartmentId=1&PageNumber=1&PageSize=10
```

---

## مرحله 6: ایجاد کاربران و پرسنل

**هدف:** ایجاد کاربران (پرسنل) و انتساب آن‌ها به دپارتمان و تخصص

### اکشن 6.1: ایجاد کاربر جدید

**Endpoint:** `POST /api/User`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "fullName": "علی احمدی",
  "phoneNumberMembership": "09123456789",
  "nationalCode": "1234567890",
  "personnelCode": "P001",
  "gender": 1,
  "dateOfEmployment": "1400/01/15",
  "isProjectPersonnel": false,
  "email": "ali.ahmadi@example.com",
  "province": "تهران",
  "city": "تهران",
  "address": "تهران، خیابان ولیعصر",
  "isActive": true,
  "canBeShiftManager": true,
  "includedProductivityPlan": true,
  "hardshipPercent": 10,
  "overtimeConsent": true,
  "departmentId": 1,
  "specialtyId": 1,
  "shiftType": 1,
  "shiftSubType": 2,
  "twoShiftRotationPattern": 1,
  "allowedShiftPermissions": 19,
  "userRoles": [2]
}
```

**توضیحات:**
- `fullName`: نام کامل کاربر
- `phoneNumberMembership`: شماره موبایل (11 رقم)
- `nationalCode`: کد ملی (10 رقم)
- `personnelCode`: کد پرسنلی
- `gender`: جنسیت
  - `0` = نامشخص
  - `1` = مرد
  - `2` = زن
- `dateOfEmployment`: تاریخ استخدام (شمسی، فرمت `yyyy/MM/dd`)
- `isProjectPersonnel`: آیا پرسنل طرحی است؟
- `canBeShiftManager`: آیا می‌تواند مسئول شیفت باشد؟
- `includedProductivityPlan`: آیا مشمول طرح بهره‌وری است؟ (برای محاسبه صحیح ساعت شیفت شب و تعطیلات)
- `departmentId`: شناسه دپارتمان
- `specialtyId`: شناسه تخصص
- `shiftType`: نوع شیفت کاربر
  - `0` = ثابت (Fixed)
  - `1` = گردشی (Rotating)
- `shiftSubType`: زیرنوع شیفت (برای گردشی/ثابت)
  - ثابت: `0`=صبح، `1`=عصر
  - گردشی: `0`=دو‌نوبته، `1`=سه‌نوبته
- `twoShiftRotationPattern`: الگوی گردشی دو‌نوبته (فقط وقتی `shiftSubType=0`)
  - `0`=صبح/عصر، `1`=صبح/شب
- `allowedShiftPermissions`: مجوزهای صریح نوع شیفت (flags، اختیاری) — اگر `null` باشد از فیلدهای بالا مشتق می‌شود
- `userRoles`: آرایه‌ای از شناسه نقش‌ها (مثلاً `[2]` برای نقش کارمند)

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "کاربر با موفقیت ایجاد شد.",
  "data": {
    "id": 1,
    "fullName": "علی احمدی",
    "departmentId": 1,
    ...
  }
}
```

### اکشن 6.2: ایجاد سوپروایزر

**Endpoint:** `POST /api/User/CreateUserSupervisorAndSendOtpForLogin`

**توضیحات:**
- این اکشن یک کاربر با نقش سوپروایزر ایجاد می‌کند و کد OTP برای لاگین ارسال می‌کند
- Body مشابه اکشن 6.1 است، اما نقش سوپروایزر به صورت خودکار تنظیم می‌شود

**Body نمونه:**
```json
{
  "fullName": "محمد رضایی",
  "phoneNumberMembership": "09187654321",
  "nationalCode": "0987654321",
  "personnelCode": "S001",
  "gender": 1,
  "dateOfEmployment": "1395/01/01",
  "isProjectPersonnel": false,
  "email": "mohammad.rezaei@example.com",
  "province": "تهران",
  "city": "تهران",
  "isActive": true,
  "canBeShiftManager": true,
  "includedProductivityPlan": true,
  "departmentId": 1,
  "specialtyId": 1,
  "shiftType": 1
}
```

### اکشن 6.3: دریافت لیست کاربران

**Endpoint:** `GET /api/User`

**مثال:**
```http
GET /api/User?DepartmentId=1&PageNumber=1&PageSize=10
```

### اکشن 6.4: تنظیمات ساعت کاری موظفی پرسنل

**هدف:** تنظیم فیلدهای مربوط به محاسبه ساعت موظفی بر اساس آیین‌نامه بهره‌وری

**نکته مهم:** سیستم به صورت خودکار ساعت موظفی ماهانه هر پرسنل را بر اساس آیین‌نامه بهره‌وری ایران محاسبه می‌کند. برای فعال‌سازی این قابلیت، باید فیلدهای زیر را در هنگام ایجاد یا ویرایش کاربر تنظیم کنید.

#### فیلدهای مهم در ایجاد/ویرایش کاربر:

**1. `includedProductivityPlan` (مشمول طرح بهره‌وری):**
- اگر `true` باشد، سیستم ساعت موظفی این کاربر را محاسبه می‌کند
- اگر `false` باشد، محاسبه ساعت موظفی انجام نمی‌شود
- **مثال:** `"includedProductivityPlan": true`

**2. `dateOfEmployment` (تاریخ استخدام):**
- برای محاسبه سابقه خدمت و کاهش ساعات موظفی بر اساس سابقه استفاده می‌شود
- باید به صورت شمسی و در قالب `yyyy/MM/dd` باشد
- **مثال:** `"dateOfEmployment": "1400/01/15"`

**3. `isProjectPersonnel` (پرسنل طرحی):**
- اگر `true` باشد، فرد **پرسنل طرحی/پروژه‌ای** است.
- در **شیفت‌بندی:** فقط تا **ساعت موظفی** شیفت می‌گیرد (**بدون اضافه‌کار**).
- پرسنل **غیرطرحی** (`false` یا `null`) در **اولویت پر کردن موظفی** جلوتر هستند و در صورت `overtimeConsent` می‌توانند اضافه‌کار بگیرند.
- این فیلد **وارد فرمول محاسبه موظفی آیین‌نامه** نمی‌شود؛ سختی کار از `hardshipPercent` محاسبه می‌شود.

**4. `maxProductivityRequiredHours` (ساعت موظفی دستی — اختیاری):**
- اگر **خالی** (`null`) یا **صفر** باشد → ساعت موظفی مثل قبل **خودکار** محاسبه می‌شود.
- اگر **مثبت** باشد → همان عدد به‌عنوان **ساعت موظفی ماهانه** اعمال می‌شود (محاسبه خودکار نادیده گرفته می‌شود).
- محدوده مجاز: `0` تا `744` ساعت.

**5. `allowedShiftPermissions` (مجوز نوع شیفت — flags، اختیاری):**
- `null` → از `shiftType` / `shiftSubType` / `twoShiftRotationPattern` مشتق می‌شود.
- `Morning=1`, `Evening=2`, `Night=4`, `MorningEveningSameDay=8`, `MorningNightSameDay=16`
- مثال مینا (صبح+عصر+صبح/شب، بدون شب تکی): `"allowedShiftPermissions": 19`

**6. `hardshipPercent` (ضریب سختی کار):**
- برای کاهش موظفی هفتگی بر اساس آیین‌نامه (حداکثر ۲ ساعت).

**7. `overtimeConsent` (رضایت اضافه‌کار):**
- اگر `false` باشد، الگوریتم اضافه‌کار به آن کاربر نمی‌دهد.

**8. `shiftType` (نوع شیفت):**
- اگر `1` (گردشی) باشد و شیفت‌های غیرمتعارف (شب و تعطیل) داشته باشد، 1 ساعت از موظفی هفتگی کسر می‌شود
- **مثال:** `"shiftType": 1`

#### نحوه محاسبه ساعت موظفی:

سیستم بر اساس آیین‌نامه بهره‌وری ایران، ساعت موظفی را به صورت زیر محاسبه می‌کند:

**1. پایه هفتگی:** 44 ساعت

**2. کاهش‌های هفتگی (حداکثر 8 ساعت):**
- **سابقه خدمت (حداکثر 5 ساعت):**
  - 1 تا 5 سال: 1 ساعت کاهش
  - 6 تا 10 سال: 2 ساعت کاهش
  - 11 تا 15 سال: 3 ساعت کاهش
  - 16 تا 20 سال: 4 ساعت کاهش
  - بیش از 20 سال: 5 ساعت کاهش
- **سختی کار (حداکثر 2 ساعت):**
  - بر اساس `hardshipPercent` (نه `isProjectPersonnel`)
- **شیفت‌های غیرمتعارف (حداکثر 1 ساعت):**
  - اگر `shiftType = 1` (گردشی) و شیفت شب/تعطیل دارد: 1 ساعت کاهش

**3. اعتبار ساعات شب/تعطیل:**
- ساعات کار در شب/تعطیل با ضریب 1.5 محاسبه می‌شود
- هر ساعت شب معادل 1.5 ساعت موظفی است
- مازاد 0.5 ساعت به عنوان "اعتبار" از موظفی ماهانه کسر می‌شود

**4. فرمول نهایی:**
```
موظفی هفتگی = 44 - (کاهش سابقه + کاهش سختی کار + کاهش شیفت گردشی)
موظفی ماهانه = (موظفی هفتگی × تعداد هفته‌ها) - اعتبار ساعات شب/تعطیل
```

**مثال محاسبه:**
- کاربر با 12 سال سابقه: کاهش 3 ساعت
- سختی کار (`hardshipPercent` مناسب): کاهش تا 2 ساعت
- شیفت گردشی با شب: کاهش 1 ساعت
- **مجموع کاهش:** 6 ساعت (کمتر از سقف 8 ساعت)
- **موظفی هفتگی:** 44 - 6 = 38 ساعت
- **موظفی ماهانه (4 هفته):** 38 × 4 = 152 ساعت
- اگر 20 ساعت شب کار کرده باشد: اعتبار = (20 × 1.5) - 20 = 10 ساعت
- **موظفی نهایی:** 152 - 10 = 142 ساعت

#### تنظیمات در تنظیمات دپارتمان:

در مرحله 7 (تنظیمات شیفت‌بندی دپارتمان)، می‌توانید رفتار سیستم در مورد ساعت موظفی را تنظیم کنید:

**1. `enforceProductivityHours` (اعمال سقف ساعات موظفی):**
- اگر `true` باشد: الگوریتم به محض رسیدن کاربر به سقف موظفی، اجازه انتساب شیفت جدید نمی‌دهد (قانون سخت)
- اگر `false` باشد: الگوریتم مجاز است ساعات مازاد را تقسیم کند، اما جریمه دریافت می‌کند (قانون نرم)
- **پیش‌فرض:** `true`

**2. `productivityOvertimeWeight` (وزن جریمه مازاد ساعات موظفی):**
- فقط زمانی اعمال می‌شود که `enforceProductivityHours = false`
- برای هر ساعت اضافه از سقف موظفی، این وزن به تابع هدف اضافه می‌شود
- **پیش‌فرض:** `2.0`
- **پیشنهاد:** برای پروژه‌های کوچک، `2.0` تا `5.0`

**مثال تنظیمات در `DepartmentSchedulingSettings`:**
```json
{
  "departmentId": 1,
  "enforceProductivityHours": true,  // یا false برای انعطاف‌پذیری
  "productivityOvertimeWeight": 2.0  // فقط اگر enforceProductivityHours = false
}
```

#### نکات مهم:

1. **فقط کاربران با `includedProductivityPlan = true`:** محاسبه ساعت موظفی فقط برای کاربرانی انجام می‌شود که این فیلد آن‌ها `true` باشد.

2. **محاسبه خودکار یا دستی:** اگر `maxProductivityRequiredHours` مقدار مثبت داشته باشد، همان عدد به‌عنوان موظفی ماهانه اعمال می‌شود؛ در غیر این صورت محاسبه خودکار انجام می‌شود.

3. **اولویت پر کردن موظفی:** پرسنل غیرطرحی قبل از طرحی؛ پرسنل طرحی فقط تا موظفی (بدون اضافه‌کار).

4. **گزارش ساعات کار شده:** در خروجی شیفت‌بندی (`ShiftSchedulingResultDto`)، می‌توانید ساعات کار شده هر کاربر را در `Statistics.WorkedHoursByUser` مشاهده کنید.

5. **مقایسه با سقف موظفی:** در `Statistics.ProductivityRequiredHoursByUser` سقف موظفی هر کاربر و در `Statistics.ProductivityOvertimeByUser` ساعات مازاد (در صورت وجود) ثبت می‌شود.

---

## مرحله 7: تنظیمات شیفت‌بندی دپارتمان

**هدف:** تنظیم قوانین سخت و نرم برای شیفت‌بندی هر دپارتمان

### اکشن 7.1: ایجاد تنظیمات شیفت‌بندی دپارتمان

**Endpoint:** `POST /api/DepartmentSchedulingSettings`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه (کامل):**
```json
{
  "departmentId": 1,
  
  // قوانین سخت (Hard Constraints)
  "forbidDuplicateDailyAssignments": true,  //ممنوعیت تکالیف تکراری روزانه
  "enforceMaxShiftsPerDay": true, //اعمال حداکثر شیفت در روز
  "enforceMinRestDays": true,   //اعمال حداقل روزهای استراحت
  "enforceMaxConsecutiveShifts": true,  //اعمال حداکثر شیفت‌های متوالی
  "enforceWeeklyMaxShifts": true,   //اعمال حداکثر شیفت‌های هفتگی
  "enforceNightShiftMonthlyCap": true,  //اعمال سقف ماهانه شیفت شب
  "enforceSpecialtyCapacity": true,   //اعمال ظرفیت تخصصی
  "enforceProductivityHours": true,   //اعمال ساعات بهره‌وری
  
  // مقادیر عددی قوانین
  "minRestDaysBetweenShifts": 1,  //حداقل روزهای استراحت بین شیفت‌ها
  "maxConsecutiveShifts": 3,  //حداکثر شیفت‌های متوالی
  "maxShiftsPerWeek": 5,  //حداکثر شیفت در هفته
  "maxNightShiftsPerMonth": 8,  //حداکثر شیفت شب در ماه
  "maxShiftsPerDay": 2, //حداکثر شیفت در روز — فقط ۱ یا ۲ مجاز است
  "maxConsecutiveNightShifts": 2, //حداکثر شیفت شب متوالی
  
  // قوانین نرم (Soft Constraints - Weights)
  "genderBalanceWeight": 1.0, //وزن تعادل جنسیتی
  "specialtyPreferenceWeight": 1.0, //وزن ترجیحی تخصصی
  "userUnwantedShiftWeight": 1.0, //وزن تغییر ناخواسته کاربر
  "userPreferredShiftWeight": 1.0,  //وزن شیفت ترجیحی کاربر
  "weeklyMaxWeight": 1.0,   //حداکثر وزن هفتگی
  "monthlyNightCapWeight": 1.0, //وزن کلاه شبانه ماهانه
  "productivityOvertimeWeight": 2.0,  //وزن اضافه کاری بهره وری
  
  // عدالت و چرخش
  "fairShiftCountBalanceWeight": 0.5, //وزن تعادل شمارش منصفانه شیفت
  "extraShiftRotationWeight": 0.8,  //وزن چرخش شیفت اضافی
  "shiftLabelBalanceWeight": 0.6, //تغییر وزن تعادل برچسب
  "fairnessLookbackMonths": 2,  //عادلانه نگاه کنید به ماه ها
  
  // تنظیمات حداقل شیفت برای پرسنل گردشی
  "enforceMinimumShiftsForRotatingStaff": true, //اجرای حداقل شیفت برای کارکنان چرخشی
  "minMorningShiftsForThreeShiftRotation": 10,  //حداقل شیفت صبح برای چرخش سه شیفت
  "minEveningShiftsForThreeShiftRotation": 10,  //حداقل شیفت عصر برای چرخش سه شیفت
  "minNightShiftsForThreeShiftRotation": 8,   //حداقل شیفت شب برای چرخش سه شیفت
  "minFirstShiftForTwoShiftRotation": 15,   //حداقل شیفت اول برای چرخش دو شیفت
  "minSecondShiftForTwoShiftRotation": 15,  //حداقل شیفت دوم برای چرخش دو شیفت
  
  // تنظیمات شب‌دوست/شب‌گریز
  "enableNightShiftPreference": false,  //فعال کردن تنظیمات شیفت شب
  "nightShiftPreferenceType": 0,   // نوع تنظیمات شب: 0=شب‌دوست، 1=شب‌گریز، 2=خنثی

  // فلگ‌های ایجاد/حذف شیفت‌بندی ماهانه
  "allowCurrentMonthScheduling": false,           // فقط توسعه/تست: اجازه Optimize برای ماه جاری
  "allowMonthlyRescheduleWithAutoDelete": false   // حذف خودکار برنامه قبلی هنگام Optimize مجدد
}
```

**توضیحات مهم:**

**قوانین سخت (Hard Constraints):**
- این قوانین باید رعایت شوند و نقض‌ناپذیرند
- `forbidDuplicateDailyAssignments`: ممنوعیت تکرار همان نوع شیفت در یک روز
- `enforceMaxShiftsPerDay`: اعمال حداکثر شیفت روزانه (`maxShiftsPerDay`)
- **`maxShiftsPerDay` فقط می‌تواند `1` یا `2` باشد** (اعتبارسنجی سخت در API)
  - `1` + `enforceMaxShiftsPerDay=true` → هر کاربر حداکثر **یک** شیفت در روز (ترکیب صبح+عصر یا صبح+شب **ممکن نیست**)
  - `2` + `enforceMaxShiftsPerDay=true` → ترکیب **صبح+عصر** یا **صبح+شب** در یک روز مجاز است؛ **عصر+شب** همچنان ممنوع
- `enforceMinRestDays`: اعمال حداقل روزهای استراحت بین شیفت‌ها
- `enforceMaxConsecutiveShifts`: اعمال حداکثر شیفت‌های متوالی
- `enforceWeeklyMaxShifts`: اعمال سقف هفتگی شیفت‌ها
- `enforceNightShiftMonthlyCap`: اعمال سقف شیفت شب ماهانه
- `enforceSpecialtyCapacity`: جلوگیری از تجاوز از ظرفیت تخصص/شیفت/روز
- `enforceProductivityHours`: اعمال سقف ساعات موظفی بهره‌وری (اگر `true` باشد، الگوریتم به محض رسیدن کاربر به سقف موظفی، اجازه انتساب شیفت جدید نمی‌دهد)

**فلگ‌های ماهانه:**
- `allowCurrentMonthScheduling`: اگر `true` باشد، برای ماه شمسی **جاری** حتی پس از شروع ماه، Optimize و حذف ماهانه مجاز است (فقط توسعه/تست)
- `allowMonthlyRescheduleWithAutoDelete`: اگر `true` باشد، هنگام Optimize جدید، برنامه قبلی همان ماه خودکار حذف و جایگزین می‌شود

**مقادیر عددی:**
- این مقادیر زمانی اعمال می‌شوند که قانون سخت مربوطه فعال باشد
- `minRestDaysBetweenShifts`: حداقل روزهای استراحت بین شیفت‌ها
- `maxConsecutiveShifts`: حداکثر شیفت‌های متوالی
- `maxShiftsPerWeek`: سقف تعداد شیفت در هفته
- `maxNightShiftsPerMonth`: سقف شیفت شب ماهانه
- `maxShiftsPerDay`: حداکثر شیفت روزانه هر نفر (**فقط ۱ یا ۲**)
- `maxConsecutiveNightShifts`: حداکثر شیفت شب متوالی

**قوانین نرم (Weights):**
- این قوانین با وزن قابل تنظیم‌اند و اهداف ترجیحی هستند
- وزن‌های بالاتر = اهمیت بیشتر
- پیشنهاد برای پروژه‌های کوچک: `0.3` تا `1.0`
- `genderBalanceWeight`: وزن تعادل جنسیتی
- `specialtyPreferenceWeight`: وزن ترجیح مطابقت تخصص
- `userUnwantedShiftWeight`: وزن جریمه شیفت‌های ناخواسته کاربر
- `userPreferredShiftWeight`: وزن پاداش شیفت‌های ترجیحی کاربر
- `weeklyMaxWeight`: وزن سقف هفتگی شیفت‌ها (نرم)
- `monthlyNightCapWeight`: وزن سقف شیفت شب ماهانه (نرم)
- `productivityOvertimeWeight`: وزن جریمه مازاد ساعات موظفی (فقط زمانی اعمال می‌شود که `enforceProductivityHours = false`)

**عدالت و چرخش:**
- `fairShiftCountBalanceWeight`: وزن تعادل تعداد شیفت بین افراد
- `extraShiftRotationWeight`: وزن چرخش شیفت‌های اضافه
- `shiftLabelBalanceWeight`: وزن تعادل Morning/Evening/Night
- `fairnessLookbackMonths`: بازه سابقه برای محاسبه عدالت بر اساس ماه

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "تنظیمات با موفقیت ایجاد شد.",
  "data": {
    "id": 1,
    "departmentId": 1,
    ...
  }
}
```

### اکشن 7.2: دریافت تنظیمات دپارتمان

**Endpoint:** `GET /api/DepartmentSchedulingSettings?DepartmentId={id}`

**مثال:**
```http
GET /api/DepartmentSchedulingSettings?DepartmentId=1
```

### اکشن 7.3: به‌روزرسانی تنظیمات

**Endpoint:** `PUT /api/DepartmentSchedulingSettings/{id}`

**Body:** مشابه اکشن 7.1

---

## مرحله 8: تنظیمات الگوریتم‌های بهینه‌سازی (اختیاری)

**هدف:** تنظیم پارامترهای الگوریتم‌های بهینه‌سازی (فقط برای ادمین)

**نکته:** این مرحله اختیاری است. اگر تنظیمات انجام نشود، سیستم از مقادیر پیش‌فرض استفاده می‌کند.

### اکشن 8.1: ایجاد تنظیمات الگوریتم Simulated Annealing

**Endpoint:** `POST /api/AlgorithmSettings`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت_ادمین}
Content-Type: application/json
```

**Body نمونه برای Simulated Annealing:**
```json
{
  "departmentId": 1,
  "algorithmType": 1,
  "sA_InitialTemperature": 1000.0,
  "sA_FinalTemperature": 0.1,
  "sA_CoolingRate": 0.95,
  "sA_MaxIterations": 10000,
  "sA_MaxIterationsWithoutImprovement": 1000
}
```

**توضیحات:**
- `departmentId`: شناسه دپارتمان (اگر `null` باشد، تنظیمات سراسری است)
- `algorithmType`: نوع الگوریتم
  - `1` = Simulated Annealing
  - `2` = OR-Tools CPSat
  - `3` = Hybrid
- `sA_InitialTemperature`: دمای اولیه
- `sA_FinalTemperature`: دمای نهایی
- `sA_CoolingRate`: نرخ خنک‌سازی
- `sA_MaxIterations`: حداکثر تعداد تکرار
- `sA_MaxIterationsWithoutImprovement`: حداکثر تکرار بدون بهبود

### اکشن 8.2: ایجاد تنظیمات الگوریتم OR-Tools

**Body نمونه:**
```json
{
  "departmentId": 1,
  "algorithmType": 2,
  "orT_MaxTimeInSeconds": 300,
  "orT_NumSearchWorkers": 4,
  "orT_LogSearchProgress": true,
  "orT_MaxSolutions": 10,
  "orT_RelativeGapLimit": 0.01
}
```

### اکشن 8.3: ایجاد تنظیمات الگوریتم Hybrid

**Body نمونه:**
```json
{
  "departmentId": 1,
  "algorithmType": 3,
  "hybrid_Strategy": 1,
  "hybrid_MaxIterations": 5,
  "hybrid_ComplexityThreshold": 50
}
```

**توضیحات Strategy:**
- `1` = OrToolsFirst
- `2` = SimulatedAnnealingFirst
- `3` = Parallel
- `4` = Iterative
- `5` = Adaptive

---

## مرحله 8.۵: سهمیه شب ماهانه

**هدف:** تعیین حداقل تعداد شیفت شب (و شب تعطیل/آخرهفته) و ترجیح مشارکت در **توزیع مازاد** برای هر کاربر در یک ماه شمسی.

سهمیه دیگر روی خود کاربر ذخیره نمی‌شود؛ برای هر ماه جداگانه با `UserMonthlyNightQuota` ثبت می‌شود.

**قبل از Optimize هر ماه**، سوپروایزر باید سهمیه را برای همان ماه شمسی تنظیم کند.

### قواعد

- مجموع `exactNightShiftCount` کاربران دپارتمان ≤ **تعداد شب‌های ماه × نفر موردنیاز شیفت شب** (بر اساس `ShiftDates` و ظرفیت تخصص)
- مجموع سهمیه شب تعطیل ≤ ظرفیت شب‌های تعطیل/آخرهفته همان ماه
- **حداکثر سهمیه هر کاربر** ≤ تعداد شب‌های همان ماه
- هر درخواست تأییدشدهٔ **حضور در شیفت شب** یک واحد از **سهمیه کل شب** همان ماه مصرف می‌کند
- ثبت سهمیه یا `fallback=true` برای کاربر **بدون مجوز شب** رد می‌شود
- در Optimize رسیدن به سهمیه حداقل **اجباری** است (اگر سهمیه قطعی تعریف شده باشد)

### فیلدها

| فیلد | معنی |
|------|------|
| `exactNightShiftCount` | حداقل تعداد شیفت شب؛ `null` = بدون کف اجباری |
| `exactHolidayWeekendNightShiftCount` | حداقل شب تعطیل/آخرهفته؛ باید ≤ کل شب |
| `nightFallbackParticipation` | `null`=مازاد (پیش‌فرض)؛ `false`=فقط قطعی؛ `true`=مشارکت صریح |
| `holidayWeekendNightFallbackParticipation` | همان منطق برای شب تعطیل/آخرهفته |

| ترکیب | رفتار در شیفت‌بندی |
|--------|---------------------|
| سهمیه `null` + fallback `null` | مشارکت در مازاد |
| سهمیه مشخص + fallback `false` | فقط همان تعداد قطعی |
| بدون رکورد | مشارکت در مازاد (پیش‌فرض) |

### اکشن‌ها (`UserMonthlyNightQuotaController`)

| اکشن | روش |
|------|------|
| `UpsertUserMonthlyNightQuota` | POST — یک کاربر |
| `UpsertDepartmentMonthlyNightQuotas` | POST — همه کاربران دپارتمان |
| `GetDepartmentMonthlyNightQuotas` | GET |
| `DeleteUserMonthlyNightQuota` | DELETE |

**بدنه نمونه — یک کاربر:**
```json
{
  "userId": 10,
  "persianYear": 1405,
  "persianMonth": 5,
  "exactNightShiftCount": 5,
  "exactHolidayWeekendNightShiftCount": 2,
  "nightFallbackParticipation": null,
  "holidayWeekendNightFallbackParticipation": false
}
```

**بدنه نمونه — یک‌جا برای دپارتمان:**
```json
{
  "departmentId": 1,
  "persianYear": 1405,
  "persianMonth": 5,
  "items": [
    {
      "userId": 3,
      "exactNightShiftCount": 5,
      "exactHolidayWeekendNightShiftCount": 2,
      "nightFallbackParticipation": null,
      "holidayWeekendNightFallbackParticipation": null
    },
    {
      "userId": 6,
      "exactNightShiftCount": 2,
      "exactHolidayWeekendNightShiftCount": 1,
      "nightFallbackParticipation": false,
      "holidayWeekendNightFallbackParticipation": null
    }
  ]
}
```

---

## مرحله 8.۶: سهمیه صبح/عصر ماهانه

**هدف:** تعیین سهمیه **جداگانه** صبح و عصر (مناسب پرسنل ثابت، گردشی سه‌نوبته، و هر کاربری که صبح/عصر مجزا دارد).

**API:** `UserMonthlyDayShiftQuotaController`

### فیلدها

| فیلد | معنی |
|------|------|
| `exactMorningShiftCount` / `morningFallbackParticipation` | سهمیه و fallback **صبح** |
| `exactHolidayMorningShiftCount` / `morningHolidayFallbackParticipation` | صبح در روزهای تعطیل |
| `exactEveningShiftCount` / `eveningFallbackParticipation` | سهمیه و fallback **عصر** |
| `exactHolidayEveningShiftCount` / `eveningHolidayFallbackParticipation` | عصر در روزهای تعطیل |

**اعتبارسنجی مجوز:** اگر کاربر مجوز صبح/عصر ندارد (مثلاً ثابت صبح)، ثبت سهمیه همان نوع **رد** می‌شود.

**الگوریتم:** ۱) برآورده کردن سهمیه‌های قطعی ۲) توزیع مازاد بین کاربران با `fallback` برابر `null` یا `true` ۳) `fallback=false` = بدون مازاد.

**بدنه نمونه:**
```json
{
  "userId": 10,
  "persianYear": 1405,
  "persianMonth": 5,
  "exactMorningShiftCount": 8,
  "morningFallbackParticipation": null,
  "exactHolidayMorningShiftCount": 2,
  "morningHolidayFallbackParticipation": null,
  "exactEveningShiftCount": 6,
  "eveningFallbackParticipation": false,
  "exactHolidayEveningShiftCount": 1,
  "eveningHolidayFallbackParticipation": null
}
```

---

## مرحله 8.۷: سهمیه ترکیبی صبح/عصر و صبح/شب

**هدف:** سهمیه **ترکیبی** برای پرسنل **گردشی** (دو‌نوبته یا **سه‌نوبته**). **پرسنل شیفت ثابت** نمی‌توانند این سهمیه را داشته باشند.

**API:** `UserMonthlyComboShiftQuotaController`

### چه موقع از کدام API استفاده کنیم؟

| نوع پرسنل | سهمیه شب | صبح/عصر جدا | سهمیه ترکیبی |
|-----------|----------|-------------|--------------|
| ثابت صبح/عصر | در صورت مجوز شب | ✅ `DayShiftQuota` | ❌ |
| گردشی دو‌نوبته صبح/عصر | ✅ `NightQuota` | اختیاری | ✅ ME |
| گردشی دو‌نوبته صبح/شب | ✅ `NightQuota` | اختیاری | ✅ MN |
| گردشی سه‌نوبته | ✅ | ✅ | ✅ (ME و MN) |

### فیلدها

| فیلد | معنی |
|------|------|
| `morningEveningShiftCount` | حداقل **مجموع** انتساب‌های صبح+عصر در ماه |
| `morningEveningFallbackParticipation` | fallback مازاد صبح/عصر |
| `morningEveningHolidayCount` / `morningEveningHolidayFallback` | تعطیل |
| `morningNightShiftCount` | حداقل **مجموع** انتساب‌های صبح+شب در ماه |
| `morningNightFallbackParticipation` | fallback مازاد صبح/شب |
| `morningNightHolidayCount` / `morningNightHolidayFallback` | تعطیل |

**بدنه نمونه:**
```json
{
  "userId": 12,
  "persianYear": 1405,
  "persianMonth": 5,
  "morningEveningShiftCount": 10,
  "morningEveningFallbackParticipation": null,
  "morningEveningHolidayCount": 2,
  "morningEveningHolidayFallback": null,
  "morningNightShiftCount": null,
  "morningNightFallbackParticipation": null,
  "morningNightHolidayCount": null,
  "morningNightHolidayFallback": null
}
```

**ترتیب پیشنهادی تنظیم سهمیه قبل از Optimize:**
1. تکمیل `ShiftDates` ماه
2. سهمیه شب + صبح/عصر + (در صورت نیاز) ترکیبی
3. ثبت/تأیید درخواست‌ها
4. `optimize-and-save`

---

## مرحله 9: ثبت درخواست‌های شیفت توسط کارکنان

**هدف:** کارکنان درخواست‌های خود را برای ترجیح شیفت یا عدم حضور ثبت می‌کنند

### اکشن 9.1: ثبت درخواست شیفت برای یک روز

**Endpoint:** `POST /api/ShiftRequest`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت}
Content-Type: application/json
```

**Body نمونه برای درخواست شیفت:**
```json
{
  "userId": 10,
  "requestPersianDate": "1404/02/05",
  "requestType": 1,
  "shiftLabel": 0,
  "requestAction": 0,
  "reason": "ترجیح می‌دهم در این تاریخ شیفت صبح داشته باشم"
}
```

**Body نمونه برای عدم حضور:**
```json
{
  "userId": 10,
  "requestPersianDate": "1404/02/10",
  "requestType": 0,
  "shiftLabel": 0,
  "requestAction": 1,
  "reason": "مرخصی استعلاجی"
}
```

**توضیحات:**
- `userId`: شناسه کاربر
- `requestPersianDate`: تاریخ درخواست (شمسی، فرمت `yyyy/MM/dd`)
- `requestType`: دامنهٔ درخواست
  - `0` = کل‌روز (`FullDay`) — **فقط** برای عدم‌حضور/مرخصی (`requestAction = 1`)
  - `1` = شیفت مشخص (`SpecificShift`) — برای حضور اجباری در یک شیفت (`requestAction = 0`) الزامی است
- **توجه:** درخواست حضور کل‌روز (`requestAction = 0` + `requestType = 0`) **مجاز نیست**. ترکیب‌های مجاز در یک روز: صبح+عصر یا صبح+شب؛ عصر+شب و شب→صبح روز بعد (متوالی بیش از ۱۲ ساعت) ممنوع‌اند.
- `shiftLabel`: نوع شیفت (برای `SpecificShift` الزامی)
  - `0` = صبح
  - `1` = عصر
  - `2` = شب
- `requestAction`: عمل درخواست
  - `0` = درخواست حضور (`RequestToBeOnShift`)
  - `1` = عدم حضور / مرخصی (`RequestToBeOffShift`)
- `reason`: دلیل درخواست

**قانون OFF تأییدشده و شب روز قبل:**
- اگر درخواست **عدم‌حضور** (`requestAction = 1`) با وضعیت **تأییدشده** برای **کل‌روز** یا **شیفت صبح** در تاریخ `D` باشد، الگوریتم **شیفت شب** روز `D−1` را به آن کاربر نمی‌دهد.
- OFF عصر یا شب فقط همان شیفت را مسدود می‌کند.

**قوانین ویژه درخواست شب (`shiftLabel = 2` + `requestAction = 0`):**
1. ابتدا سهمیه شب ماهانه کاربر باید ثبت شده باشد
2. تعداد درخواست‌های شب تأییدشده + این درخواست نباید از `exactNightShiftCount` بیشتر شود
3. برای شب تعطیل/آخرهفته، محدودیت `exactHolidayWeekendNightShiftCount` هم اعمال می‌شود (در صورت تعریف)

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "درخواست با موفقیت ثبت شد.",
  "data": {
    "id": 1,
    "status": 0,
    ...
  }
}
```

**نکته:** وضعیت درخواست به صورت پیش‌فرض `Pending` (0) است و باید توسط سوپروایزر تأیید شود.

### اکشن 9.2: ثبت درخواست مرخصی برای بازه زمانی

**Endpoint:** `POST /api/ShiftRequest/CreateShiftRequestForLeave`

**Body نمونه:**
```json
{
  "userId": 10,
  "startPersianDate": "1404/02/10",
  "endPersianDate": "1404/02/15",
  "reason": "مرخصی استعلاجی"
}
```

**توضیحات:**
- این اکشن برای تمام روزهای بازه (از `startPersianDate` تا `endPersianDate`) درخواست عدم حضور ایجاد می‌کند
- تمام درخواست‌ها با وضعیت `Pending` ایجاد می‌شوند

### اکشن 9.3: دریافت لیست درخواست‌های شیفت

**Endpoint:** `GET /api/ShiftRequest`

**مثال:**
```http
GET /api/ShiftRequest?UserId=10&Status=0&PageNumber=1&PageSize=10
```

**توضیحات Status:**
- `0` = Pending (در انتظار بررسی)
- `1` = Approved (تأیید شده)
- `2` = Rejected (رد شده)

---

## مرحله 10: بررسی و تأیید درخواست‌های شیفت

**هدف:** سوپروایزر درخواست‌های کارکنان را بررسی و تأیید یا رد می‌کند

**نکته مهم:** فقط درخواست‌های با وضعیت `Approved` در فرآیند شیفت‌بندی لحاظ می‌شوند.

**لغو تأیید اشتباه:** اگر سوپروایزر درخواستی را به‌اشتباه تأیید کرده باشد، می‌تواند همان اکشن را با `status: 2` (`Rejected`) صدا بزند تا تأیید لغو شود. درخواست‌های ردشده با `DELETE /api/ShiftRequest/DeleteShiftRequest?id={id}` قابل حذف‌اند؛ درخواست تأییدشده مستقیماً حذف نمی‌شود.

### اکشن 10.1: تأیید / رد / لغو تأیید درخواست شیفت

**Endpoint:** `PUT /api/ShiftRequest/UpdateShiftRequestBySupervisor?id={id}`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت_سوپروایزر}
Content-Type: application/json
```

**Body نمونه (تأیید):**
```json
{
  "status": 1,
  "supervisorComment": "درخواست تأیید شد"
}
```

**Body نمونه (رد یا لغو تأیید):**
```json
{
  "status": 2,
  "supervisorComment": "تأیید قبلی اشتباه بود"
}
```

**توضیحات:**
- `status`: وضعیت جدید
  - `1` = Approved (تأیید) — فقط از `Pending`
  - `2` = Rejected (رد) — از `Pending` یا از `Approved` (لغو تأیید)
- `supervisorComment`: یادداشت سوپروایزر

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "درخواست با موفقیت تأیید شد.",
  "data": {
    "id": 1,
    "status": 1
  }
}
```

### اکشن 10.1ب: حذف درخواست ردشده یا در انتظار

**Endpoint:** `DELETE /api/ShiftRequest/DeleteShiftRequest?id={id}`

- `Pending` و `Rejected` قابل حذف‌اند.
- `Approved` قابل حذف نیست؛ ابتدا رد کنید.

### اکشن 10.2: دریافت لیست درخواست‌های در انتظار بررسی

**Endpoint:** `GET /api/ShiftRequest?Status=0&DepartmentId={id}`

**مثال:**
```http
GET /api/ShiftRequest?Status=0&DepartmentId=1&PageNumber=1&PageSize=50
```

**نکته:** قبل از اجرای شیفت‌بندی، باید تمام درخواست‌های `Pending` بررسی و تأیید یا رد شوند.

### محدودیت ظرفیت هنگام تأیید حضور

سوپروایزر **نمی‌تواند** درخواست **حضور در شیفت مشخص** (`requestAction = 0` + `requestType = 1`) را بیش از **ظرفیت همان شیفت برای تخصص کاربر** در همان روز تأیید کند.

- ظرفیت از `ShiftRequiredSpecialty` خوانده می‌شود (`requiredTottalCount` / `holidayRequiredTottalCount`).
- فقط هنگام `UpdateShiftRequestBySupervisor` با `status: 1` از `Pending` اعمال می‌شود.
- درخواست‌های OFF، کل‌روز، یا لغو تأیید تحت این قاعده نیستند.
- در صورت پر بودن ظرفیت، API با `isSuccess: false` و پیام فارسی مناسب برمی‌گردد.

---

## مرحله 11: اجرای عملیات شیفت‌بندی

**هدف:** اجرای الگوریتم بهینه‌سازی برای تخصیص شیفت‌ها به پرسنل

### اکشن 11.1: اعتبارسنجی محدودیت‌ها (اختیاری اما توصیه می‌شود)

**Endpoint:** `POST /api/ShiftScheduling/validate`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت_سوپروایزر}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "departmentId": 1,
  "startDate": "1404/02/01",
  "endDate": "1404/02/31",
  "algorithm": 1
}
```

**توضیحات:**
- `departmentId`: شناسه دپارتمان
- `startDate`: تاریخ شروع بازه (شمسی، فرمت `yyyy/MM/dd`)
- `endDate`: تاریخ پایان بازه (شمسی، فرمت `yyyy/MM/dd`)
- `algorithm`: الگوریتم انتخابی
  - `1` = Simulated Annealing
  - `2` = OR-Tools CPSat
  - `3` = Hybrid

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "data": []
}
```

اگر خطایی وجود داشته باشد:
```json
{
  "isSuccess": true,
  "data": [
    "درخواست‌های Pending در بازه وجود دارد",
    "ظرفیت تخصص کافی نیست"
  ]
}
```

### اکشن 11.2: اجرای بهینه‌سازی شیفت‌بندی

**Endpoint:** `POST /api/ShiftScheduling/optimize`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت_سوپروایزر}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "departmentId": 1,
  "startDate": "1404/02/01",
  "endDate": "1404/02/31",
  "algorithm": 1
}
```

**توضیحات:**
- این اکشن الگوریتم بهینه‌سازی را اجرا می‌کند
- سیستم به صورت خودکار:
  - کاربران فعال دپارتمان را بارگذاری می‌کند
  - **سهمیه‌های ماهانه** را برای ماه شمسی شروع بازه بارگذاری می‌کند:
    - شب (`UserMonthlyNightQuota`) — حداقل شب **اجباری**
    - صبح/عصر (`UserMonthlyDayShiftQuota`) — حداقل صبح/عصر **اجباری**
    - ترکیبی (`UserMonthlyComboShiftQuota`) — حداقل ME/MN **اجباری**
  - درخواست‌های تأیید شده در بازه را اعمال می‌کند
  - قوانین سخت و نرم را از پایگاه داده می‌خواند
  - پارامترهای الگوریتم را از تنظیمات ادمین (یا پیش‌فرض) می‌خواند
  - در صورت فعال بودن `enforceMaxShiftsPerDay`، سقف روزانه (`maxShiftsPerDay` = ۱ یا ۲) را اعمال می‌کند
- اگر سهمیه حداقل (شب، صبح/عصر، یا ترکیبی) برای کاربری برآورده نشود، پاسخ با `isSuccess: false` و پیام خطا برمی‌گردد

**ترکیب مجاز شیفت در یک روز (در خروجی هم رعایت می‌شود):** صبح+عصر یا صبح+شب؛ عصر+شب و شب→صبح روز بعد ممنوع.

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "data": {
    "algorithmUsed": 1,
    "algorithmStatus": "Completed",
    "finalScore": 1250.5,
    "totalAssignments": 180,
    "hardConstraintViolations": 0,
    "softConstraintScore": 1250.5,
    "assignments": [
      {
        "userId": 10,
        "shiftId": 1,
        "shiftDateId": 15,
        "persianDate": "1404/02/01",
        "shiftLabel": 0
      },
      ...
    ],
    "statistics": {
      "totalIterations": 5000,
      "executionTime": "00:02:30",
      ...
    }
  }
}
```

**نکته:** این اکشن فقط نتیجه بهینه‌سازی را برمی‌گرداند و در پایگاه داده ذخیره نمی‌کند.

### اکشن 11.3: مشاهده آمار الگوریتم

**Endpoint:** `POST /api/ShiftScheduling/statistics`

**Body:** مشابه اکشن 11.2

**توضیحات:**
- این اکشن آمار و اطلاعات الگوریتم را بدون اجرای کامل بهینه‌سازی برمی‌گرداند

### اکشن 11.4: ذخیره نتیجه بهینه‌سازی

**Endpoint:** `POST /api/ShiftScheduling/save`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت_سوپروایزر}
Content-Type: application/json
```

**Body:** خروجی اکشن 11.2 را ارسال کنید

**توضیحات:**
- این اکشن نتیجه بهینه‌سازی را در پایگاه داده ذخیره می‌کند
- انتساب‌های قبلی در بازه حذف می‌شوند و نتایج جدید ذخیره می‌شوند

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "برنامه شیفت‌بندی با موفقیت ذخیره شد.",
  "data": "180 انتساب شیفت ذخیره شد"
}
```

### اکشن 11.5: اجرای کامل (بهینه‌سازی + ذخیره)

**Endpoint:** `POST /api/ShiftScheduling/optimize-and-save`

**Headers:**
```
Authorization: Bearer {توکن_احراز_هویت_سوپروایزر}
Content-Type: application/json
```

**Body نمونه:**
```json
{
  "departmentId": 1,
  "startDate": "1404/02/01",
  "endDate": "1404/02/31",
  "algorithm": 1
}
```

**توضیحات:**
- این اکشن به صورت خودکار:
  1. اعتبارسنجی می‌کند
  2. بهینه‌سازی را اجرا می‌کند
  3. نتیجه را ذخیره می‌کند

**خروجی موفق:**
```json
{
  "isSuccess": true,
  "message": "Shift schedule optimized and saved successfully",
  "data": {
    "optimizationResult": { ... },
    "saveResult": "180 انتساب شیفت ذخیره شد"
  }
}
```

### اکشن 11.6: حذف شیفت‌بندی یک ماه

**Endpoint:** `POST /DeleteMonthlySchedule`

```json
{
  "departmentId": 1,
  "persianYear": 1405,
  "persianMonth": 5
}
```

- همه انتساب‌های دپارتمان در آن ماه شمسی حذف می‌شود
- فقط تا قبل از شروع ماه مجاز است؛ مگر `allowCurrentMonthScheduling = true`
- اگر `allowMonthlyRescheduleWithAutoDelete = true` باشد، معمولاً نیازی به فراخوانی دستی این اکشن قبل از Optimize نیست

---

## نکات مهم و خطاهای رایج

### ✅ چک‌لیست قبل از اجرای شیفت‌بندی

- [ ] تقویم سال ایجاد شده است
- [ ] بیمارستان و دپارتمان ایجاد شده‌اند
- [ ] تخصص‌ها تعریف شده‌اند
- [ ] شیفت‌ها با ظرفیت‌های تخصصی تعریف شده‌اند
- [ ] کاربران ایجاد و به دپارتمان/تخصص انتساب داده شده‌اند
- [ ] فیلدهای مربوط به ساعت موظفی (`includedProductivityPlan`, `dateOfEmployment`, `hardshipPercent`, `isProjectPersonnel`, `maxProductivityRequiredHours`) برای کاربران تنظیم شده‌اند
- [ ] تنظیمات شیفت‌بندی دپارتمان انجام شده است (شامل `enforceProductivityHours`، `enforceMaxShiftsPerDay` / `maxShiftsPerDay` فقط ۱ یا ۲، و در صورت نیاز فلگ‌های ماه جاری / حذف خودکار)
- [ ] **سهمیه شب ماهانه** برای ماه موردنظر ثبت شده و مجموع سهمیه‌ها ≤ ظرفیت شب ماه است
- [ ] **سهمیه صبح/عصر ماهانه** (در صورت استفاده) ثبت شده و با مجوز هر کاربر سازگار است
- [ ] **سهمیه ترکیبی ME/MN** (برای پرسنل گردشی) ثبت شده؛ پرسنل ثابت سهمیه ترکیبی ندارند
- [ ] تمام درخواست‌های `Pending` بررسی و تأیید/رد شده‌اند
- [ ] درخواست‌های شب تأییدشده از سهمیه ماهانه هر کاربر بیشتر نیست
- [ ] اگر برنامه قبلی برای همان ماه وجود دارد: یا حذف شده (`DeleteMonthlySchedule`) یا `allowMonthlyRescheduleWithAutoDelete` فعال است

### ⚠️ خطاهای رایج و راه حل

#### خطا: "There are pending shift requests in the selected period"

**علت:** درخواست‌های در وضعیت `Pending` در بازه زمانی وجود دارد.

**راه حل:**
1. لیست درخواست‌های `Pending` را دریافت کنید: `GET /api/ShiftRequest?Status=0&DepartmentId={id}`
2. هر درخواست را بررسی و تأیید یا رد کنید: `PUT /api/ShiftRequest/UpdateShiftRequestBySupervisor/{id}`

#### خطا مربوط به سهمیه شب / درخواست شب

**نمونه‌ها:**
- «برای ثبت/تأیید درخواست شیفت شب، ابتدا سهمیه شب ماهانه...»
- «سهمیه شیفت شب این کاربر ... امکان ثبت درخواست شب بیشتر از سهمیه وجود ندارد»
- «مجموع سهمیه شیفت شب کاربران ... از تعداد شب‌های ماه بیشتر است»
- «سهمیه حداقل شیفت شب برای همه کاربران اعمال نشد»

**راه حل:**
1. سهمیه ماهانه را با `UpsertDepartmentMonthlyNightQuotas` تنظیم/کاهش دهید تا مجموع ≤ ظرفیت ماه باشد
2. درخواست‌های شب اضافی را رد کنید یا سهمیه کاربر را افزایش دهید (بدون عبور از ظرفیت کل ماه)
3. دوباره Optimize را اجرا کنید

#### خطا: سقف شیفت روزانه (`maxShiftsPerDay`)

**نمونه‌ها:**
- «حداکثر شیفت در روز فقط می‌تواند ۱ یا ۲ باشد»
- «کاربر ... بیش از حداکثر مجاز شیفت در یک روز دارد»

**راه حل:**
1. در تنظیمات دپارتمان `maxShiftsPerDay` را فقط `1` یا `2` بگذارید
2. اگر `enforceMaxShiftsPerDay=true` و مقدار `1` است، ترکیب صبح+عصر یا صبح+شب در یک روز غیرممکن می‌شود
3. برای مجوز ترکیبی در کاربر، `maxShiftsPerDay` باید `2` باشد

#### خطا: سهمیه صبح/عصر یا ترکیبی / نوع پرسنل

**نمونه‌ها:**
- «پرسنل شیفت ثابت نمی‌تواند سهمیه ترکیبی داشته باشد»
- «کاربر مجوز شیفت صبح/عصر ندارد»
- «سهمیه حداقل صبح/عصر (یا ME/MN) برای همه کاربران اعمال نشد»

**راه حل:**
1. نوع پرسنل (`shiftType` / `shiftSubType`) و `allowedShiftPermissions` را با نوع سهمیه هم‌تراز کنید
2. پرسنل **ثابت** → `UserMonthlyDayShiftQuota` (نه Combo)
3. پرسنل **گردشی** (دو‌نوبته یا **سه‌نوبته**) → Combo در صورت نیاز + NightQuota
4. سهمیه‌ها را با `UpsertDepartmentMonthlyDayShiftQuotas` / `UpsertDepartmentMonthlyComboShiftQuotas` اصلاح کنید

#### خطا: برنامه قبلی / ماه شروع‌شده

**نمونه‌ها:** پیام‌های مربوط به وجود شیفت‌بندی ذخیره‌شده یا شروع شدن ماه شمسی.

**راه حل:**
1. با `POST /DeleteMonthlySchedule` برنامه قبلی را حذف کنید (فقط قبل از شروع ماه، مگر `allowCurrentMonthScheduling` روشن باشد)
2. یا `allowMonthlyRescheduleWithAutoDelete` را در تنظیمات دپارتمان فعال کنید
3. برای تست ماه جاری هر دو فلگ `allowCurrentMonthScheduling` و `allowMonthlyRescheduleWithAutoDelete` را روشن کنید

#### خطا: "Calendar not found" یا "ShiftDate not found"

**علت:** تقویم سال ایجاد نشده است.

**راه حل:**
1. تقویم سال را ایجاد کنید: `POST /api/CalendarSeeder/SeedYear?year=1404`

#### خطا: "Insufficient capacity" یا "Specialty capacity exceeded"

**علت:** ظرفیت تخصصی تعریف شده برای شیفت‌ها کافی نیست یا تعداد کاربران با تخصص مورد نیاز کم است.

**راه حل:**
1. ظرفیت‌های تخصصی شیفت‌ها را بررسی کنید: `GET /api/Shift?DepartmentId={id}`
2. تعداد کاربران با تخصص مورد نیاز را افزایش دهید
3. یا ظرفیت‌های تخصصی را کاهش دهید

#### خطا: "No active users in department"

**علت:** هیچ کاربر فعالی در دپارتمان وجود ندارد.

**راه حل:**
1. کاربران را بررسی کنید: `GET /api/User?DepartmentId={id}&IsActive=true`
2. کاربران غیرفعال را فعال کنید: `PUT /api/User/{id}` با `"isActive": true`

#### خطا: "Invalid date format"

**علت:** فرمت تاریخ اشتباه است.

**راه حل:**
- تاریخ‌ها باید به صورت شمسی و در قالب `yyyy/MM/dd` باشند (مثلاً `1404/02/15`)
- از فرمت میلادی استفاده نکنید

### 📝 نکات مهم

1. **اولویت اعمال مقادیر عددی:**
   - اگر در بدنه درخواست `optimize` برای کاربر مقدار داده شود، همان اعمال می‌شود
   - در غیر این صورت اگر در تنظیمات دپارتمان مقدار ثبت شده و `Enforce*` مربوط روشن باشد، مقدار دپارتمان اعمال می‌شود
   - در غیر این صورت مقادیر پیش‌فرض سیستم استفاده می‌شود

2. **وزن‌های قوانین نرم:**
   - اگر می‌خواهید عدالت و چرخش قوی‌تر عمل کند، وزن‌ها را افزایش دهید
   - برای پروژه‌های کوچک، وزن‌های `0.3` تا `1.0` پیشنهاد می‌شود

3. **بازه زمانی:**
   - حداکثر طول بازه زمان‌بندی 62 روز است

4. **الگوریتم‌ها:**
   - **Simulated Annealing (1):** مناسب برای موارد متوسط تا بزرگ، سریع‌تر
   - **OR-Tools CPSat (2):** مناسب برای موارد کوچک تا متوسط، دقیق‌تر
   - **Hybrid (3):** ترکیبی از هر دو، مناسب برای موارد پیچیده

5. **ساعت موظفی بهره‌وری:**
   - سیستم به صورت خودکار ساعت موظفی هر کاربر را بر اساس آیین‌نامه بهره‌وری محاسبه می‌کند، **مگر** `maxProductivityRequiredHours` مقدار مثبت داشته باشد.
   - برای فعال‌سازی، فیلد `includedProductivityPlan` باید `true` باشد.
   - محاسبه بر اساس: سابقه خدمت، `hardshipPercent`، نوع شیفت (`shiftType`) و ساعات شب/تعطیل.
   - **اولویت پر کردن موظفی:** غیرطرحی قبل از طرحی؛ پرسنل طرحی (`isProjectPersonnel = true`) فقط تا موظفی، بدون اضافه‌کار.
   - در تنظیمات دپارتمان، `enforceProductivityHours` تعیین می‌کند که آیا سقف موظفی به صورت سخت اعمال شود یا نرم (با جریمه)

6. **OFF تأییدشده و استراحت شب قبل:**
   - OFF تأییدشده برای **صبح** یا **کل‌روز** → **شب روز قبل** به کاربر داده نمی‌شود.

7. **سهمیه ماهانه (شب، صبح/عصر، ترکیبی):**
   - سهمیه‌ها را قبل از درخواست شب و قبل از Optimize تنظیم کنید
   - مجموع سهمیه‌ها نباید از ظرفیت ماه (بر اساس `ShiftDates` و نیازمندی تخصص) بیشتر شود
   - Optimize در صورت نرسیدن به سهمیه حداقل، شکست می‌خورد
   - فیلدهای `*FallbackParticipation`: `null`=مازاد، `false`=فقط قطعی، `true`=مشارکت صریح

8. **ترکیب شیفت یک روز:**
   - مجاز: صبح+عصر ، صبح+شب
   - ممنوع: عصر+شب ، شب→صبح روز بعد ، شب (D−1) + OFF صبح/کل‌روز (D)

---

## خلاصه مراحل

1. ✅ **ایجاد تقویم** → `POST /api/CalendarSeeder/SeedYear?year=1405`
2. ✅ **ایجاد بیمارستان** → `POST /api/Hospital`
3. ✅ **ایجاد دپارتمان** → `POST /api/Department`
4. ✅ **تعریف تخصص‌ها** → `POST /api/Specialty`
5. ✅ **تعریف شیفت‌ها** → `POST /api/Shift`
6. ✅ **ایجاد کاربران** → `POST /api/User`
7. ✅ **تنظیمات دپارتمان** → `POST /api/DepartmentSchedulingSettings` (در صورت نیاز فلگ‌های ماهانه)
8. ✅ **تنظیمات الگوریتم (اختیاری)** → `POST /api/AlgorithmSettings`
9. ✅ **سهمیه شب ماهانه** → `POST /UpsertDepartmentMonthlyNightQuotas`
10. ✅ **سهمیه صبح/عصر ماهانه (در صورت نیاز)** → `POST /UpsertDepartmentMonthlyDayShiftQuotas`
11. ✅ **سهمیه ترکیبی ME/MN (پرسنل گردشی)** → `POST /UpsertDepartmentMonthlyComboShiftQuotas`
12. ✅ **ثبت درخواست‌های شیفت** → `POST /api/ShiftRequest` (شب فقط داخل سهمیه)
13. ✅ **تأیید درخواست‌ها** → `PUT /api/ShiftRequest/UpdateShiftRequestBySupervisor/{id}`
14. ✅ **حذف برنامه قبلی (در صورت نیاز)** → `POST /DeleteMonthlySchedule`
15. ✅ **اجرای شیفت‌بندی** → `POST /api/ShiftScheduling/optimize-and-save`

---

**پایان راهنما**

برای جزئیات فرانت‌اند به `Docs/FrontendGuide_ProductivityAndScheduling_FA.md` مراجعه کنید.
برای سوالات بیشتر یا مشکلات فنی، با تیم پشتیبانی تماس بگیرید.
