# راهنمای کاربری شیفت‌بندی با ShiftYar

این راهنما برای **سوپروایزر، برنامه‌ریز شیفت و مدیر سیستم** نوشته شده است؛ از تعریف بیمارستان و بخش تا اجرای الگوریتم **Simulated Annealing (SA)** و ذخیره برنامه نهایی.

> **تاریخ‌ها:** همه تاریخ‌های ورودی API باید **شمسی** و به قالب `yyyy/MM/dd` باشند (مثلاً `1404/07/01`).

> **راهنمای تکمیلی:** جزئیات API و نمونه‌های بیشتر در فایل [`راهنمای_کامل_فرآیند_شیفت_بندی.md`](راهنمای_کامل_فرآیند_شیفت_بندی.md) موجود است.

---

## فهرست مطالب

1. [نمای کلی فرآیند](#۱-نمای-کلی-فرآیند)
2. [پیش‌نیازها](#۲-پیش‌نیازها)
3. [مرحله ۱: تقویم](#۳-مرحله-۱-تقویم)
4. [مرحله ۲: بیمارستان](#۴-مرحله-۲-بیمارستان)
5. [مرحله ۳: دپارتمان (بخش)](#۵-مرحله-۳-دپارتمان-بخش)
6. [مرحله ۴: تخصص‌ها](#۶-مرحله-۴-تخصصها)
7. [مرحله ۵: شیفت‌ها](#۷-مرحله-۵-شیفتها)
8. [مرحله ۶: ظرفیت تخصصی هر شیفت](#۸-مرحله-۶-ظرفیت-تخصصی-هر-شیفت)
9. [مرحله ۷: کاربران و پرسنل](#۹-مرحله-۷-کاربران-و-پرسنل)
10. [مرحله ۸: تنظیمات زمان‌بندی دپارتمان](#۱۰-مرحله-۸-تنظیمات-زمان‌بندی-دپارتمان)
11. [مرحله ۹: تنظیمات الگوریتم](#۱۱-مرحله-۹-تنظیمات-الگوریتم)
12. [مرحله ۱۰: درخواست‌های شیفت پرسنل](#۱۲-مرحله-۱۰-درخواستهای-شیفت-پرسنل)
13. [مرحله ۱۱: اجرای شیفت‌بندی](#۱۳-مرحله-۱۱-اجرای-شیفت‌بندی)
14. [تفسیر خروجی و عیب‌یابی](#۱۴-تفسیر-خروجی-و-عیبیابی)
15. [پیشنهاد تنظیمات بر اساس اندازه بخش](#۱۵-پیشنهاد-تنظیمات-بر-اساس-اندازه-بخش)

---

## ۱. نمای کلی فرآیند

```
تقویم → بیمارستان → دپارتمان → تخصص → شیفت → ظرفیت تخصصی
    → کاربران → تنظیمات دپارتمان → تنظیمات الگوریتم
    → تأیید درخواست‌های شیفت → validate → optimize → save
```

**ترتیب مهم است.** اگر ظرفیت شیفت یا پرسنل فعال تعریف نشده باشد، الگوریتم نتیجه معناداری تولید نمی‌کند.

---

## ۲. پیش‌نیازها

| مورد | توضیح |
|------|--------|
| پایگاه داده | SQL Server و اجرای Migrationها |
| API | سرویس ShiftYar.Api در حال اجرا |
| نقش دسترسی | **Admin** یا **Supervisor** |
| احراز هویت | توکن JWT در هدر `Authorization: Bearer {token}` |
| بازه شیفت‌بندی | حداکثر **۶۲ روز** در هر درخواست |

---

## ۳. مرحله ۱: تقویم

**هدف:** ثبت روزهای سال شمسی و تعطیلات برای نگاشت تاریخ‌ها.

```http
POST /api/CalendarSeeder/SeedYear?year=1404
```

**قبل از اجرا:** فایل تعطیلات را در مسیر زیر قرار دهید:

```
ShiftYar.Api/Resources/Holidays/holidays_1404.json
```

**بررسی:**

```http
GET /api/CalendarSeeder?PersianDateStart=1404/07/01&PersianDateEnd=1404/07/10
```

---

## ۴. مرحله ۲: بیمارستان

**Endpoint:** `POST /api/Hospital`

```json
{
  "siamCode": "12345",
  "name": "بیمارستان نمونه",
  "province": "تهران",
  "city": "تهران",
  "address": "آدرس بیمارستان",
  "phoneNumbers": [{ "phoneNumber": "021-12345678" }],
  "email": "info@hospital.com",
  "isActive": true
}
```

| فیلد | توضیح |
|------|--------|
| `siamCode` | کد سیام (شناسه یکتا) |
| `name` | نام بیمارستان |
| `isActive` | فقط بیمارستان‌های فعال در جریان کار استفاده می‌شوند |

---

## ۵. مرحله ۳: دپارتمان (بخش)

**Endpoint:** `POST /api/Department`

```json
{
  "name": "بخش داخلی",
  "hospitalId": 1,
  "supervisorId": 1,
  "isActive": true,
  "isNightLover": false,
  "description": "بخش بستری داخلی"
}
```

| فیلد | توضیح | تأثیر بر شیفت‌بندی |
|------|--------|-------------------|
| `hospitalId` | بیمارستان والد | الزامی |
| `supervisorId` | سوپروایزر بخش | مدیریت درخواست‌های شیفت |
| `isNightLover` | بخش شب‌دوست؟ | `true` → سخت‌گیری کمتر روی سقف شب؛ `false` → سخت‌گیری بیشتر (پیش‌فرض وزن شب: ۰.۵ در برابر ۲.۰) |
| `isActive` | وضعیت فعال | فقط بخش‌های فعال |

---

## ۶. مرحله ۴: تخصص‌ها

هر بخش تخصص‌های خود را دارد (مثلاً پرستار، دستیار، متخصص).

**Endpoint:** `POST /api/Specialty`

```json
{
  "specialtyName": "پرستار",
  "departmentId": 1,
  "isActive": true
}
```

> هر کاربر باید به **یک تخصص** در همان بخش متصل باشد. الگوریتم فقط پرسنل با تخصص مطابق را در شیفت همان تخصص قرار می‌دهد.

---

## ۷. مرحله ۵: شیفت‌ها

برای هر بخش، شیفت‌های روزانه تعریف کنید.

**Endpoint:** `POST /api/Shift`

```json
{
  "label": 0,
  "departmentId": 1,
  "startTime": "07:00:00",
  "endTime": "15:00:00",
  "isActive": true
}
```

| `label` | معنی |
|---------|------|
| `0` | صبح (Morning) |
| `1` | عصر (Evening) |
| `2` | شب (Night) |

معمولاً هر بخش سه شیفت صبح/عصر/شب دارد. ساعت شروع و پایان در محاسبه **ساعات موظفی بهره‌وری** استفاده می‌شود.

---

## ۸. مرحله ۶: ظرفیت تخصصی هر شیفت

**مهم‌ترین مرحله برای کیفیت برنامه.** مشخص می‌کند در هر شیفت، از هر تخصص چند نفر (و با چه جنسیتی) لازم است.

**Endpoint:** `POST /api/ShiftRequiredSpecialty`

```json
{
  "shiftId": 1,
  "specialtyId": 1,
  "requiredMaleCount": 1,
  "requiredFemaleCount": 1,
  "requiredTottalCount": 2,
  "onCallMaleCount": 0,
  "onCallFemaleCount": 0,
  "onCallTottalCount": 1
}
```

### فیلدهای نیروی حاضر (عادی)

| فیلد | توضیح |
|------|--------|
| `requiredMaleCount` | تعداد **مرد** مورد نیاز (صریح) |
| `requiredFemaleCount` | تعداد **زن** مورد نیاز (صریح) |
| `requiredTottalCount` | **مجموع** نیروی عادی مورد نیاز |

### فیلدهای آنکال

| فیلد | توضیح |
|------|--------|
| `onCallMaleCount` | تعداد مرد آنکال (صریح) |
| `onCallFemaleCount` | تعداد زن آنکال (صریح) |
| `onCallTottalCount` | مجموع نیروی آنکال |

### قوانین ترکیب جنسیت (بسیار مهم)

| حالت تنظیم | رفتار الگوریتم |
|------------|----------------|
| `requiredMaleCount > 0` یا `requiredFemaleCount > 0` | ترکیب جنسیت **الزامی** است؛ مثلاً ۱ مرد + ۱ زن |
| فقط `requiredTottalCount` (بدون تعداد صریح مرد/زن) | هر ترکیبی از جنسیت‌ها مجاز است؛ فقط **تعداد کل** مهم است |
| آنکال با `onCallMaleCount` / `onCallFemaleCount` | همان منطق برای نیروی آنکال |
| `onCallTottalCount > 0` | پرسنل آنکال با `IsOnCall = true` ذخیره می‌شوند |

**مثال‌های رایج بیمارستانی:**

```
بخش زنان:     requiredFemaleCount=2, requiredTottalCount=2
بخش مختلط:    requiredMaleCount=1, requiredFemaleCount=1, requiredTottalCount=2
بخش عمومی:    requiredTottalCount=3  (بدون محدودیت جنسیت)
آنکال:        onCallTottalCount=1    (یک نفر آنکال، هر جنسیت)
```

> **توجه:** در پایگاه داده فیلد مجموع با نام `requiredTottalCount` (با دو t) ذخیره می‌شود.

---

## ۹. مرحله ۷: کاربران و پرسنل

**Endpoint:** `POST /api/User`

```json
{
  "fullName": "علی احمدی",
  "phoneNumberMembership": "09123456789",
  "nationalCode": "1234567890",
  "personnelCode": "P001",
  "gender": 0,
  "dateOfEmployment": "1400/01/15",
  "isProjectPersonnel": false,
  "isActive": true,
  "canBeShiftManager": true,
  "includedProductivityPlan": true,
  "departmentId": 1,
  "specialtyId": 1,
  "shiftType": 1,
  "userRoles": [2]
}
```

### فیلدهای کلیدی

| فیلد | مقادیر | توضیح |
|------|--------|--------|
| `gender` | `0` = مرد، `1` = زن | برای قوانین جنسیت شیفت |
| `specialtyId` | شناسه تخصص | تعیین می‌کند در کدام شیفت‌ها قابل انتساب است |
| `shiftType` | `0` = ثابت، `1` = گردشی | پرسنل گردشی در تعادل صبح/عصر/شب دخیل‌اند |
| `canBeShiftManager` | true/false | مسئول شیفت (برای قوانین آینده مسئول شیفت) |
| `includedProductivityPlan` | true/false | اگر true باشد، سقف ساعات موظفی محاسبه و اعمال می‌شود |
| `dateOfEmployment` | تاریخ شمسی | برای محاسبه سابقه و ساعات موظفی |
| `hardshipPercent` | عدد اعشاری | ضریب سختی کار (۰ تا ۱۰۰) برای کاهش موظفی |
| `overtimeConsent` | true/false | اگر false باشد، اضافه‌کار در شیفت‌بندی مجاز نیست |
| `isProjectPersonnel` | true/false/null | پرسنل **طرحی** (`true`): فقط تا موظفی، بدون اضافه‌کار؛ **غیرطرحی** اول پر می‌شوند |
| `maxProductivityRequiredHours` | عدد یا null | ساعت موظفی **دستی** ماهانه؛ `null`/۰ = محاسبه خودکار |
| `allowedShiftPermissions` | flags (int) یا null | مجوز نوع شیفت؛ `null` = مشتق از `shiftType` |
| `isActive` | true/false | فقط کاربران فعال در شیفت‌بندی شرکت می‌کنند |

#### مجوز نوع شیفت (`allowedShiftPermissions`)

پنج نوع مجاز (flags): **صبح (1)**، **عصر (2)**، **شب (4)**، **صبح/عصر (8)**، **صبح/شب (16)**.  
ترکیب‌های روزانه فقط با `maxShiftsPerDay = 2` در تنظیمات دپارتمان.  
مثال: `19` = صبح + عصر + صبح/شب (بدون شب تکی و بدون صبح/عصر).

> **قانون اولویت قطعی درخواست‌های تأییدشده بر مجوزها:**  
> اگر برای کاربری درخواست شیفت تأییدشده‌ای ثبت شود که نوع آن شیفت طبق مجوزهای فردی (`allowedShiftPermissions` یا `shiftSubType`) در حالت عادی مجاز نباشد (مثلاً کاربر گردشی فقط مجاز به شیفت‌های عصر و شب است، اما درخواست تأییدشده شیفت صبح دارد)، **درخواست تأییدشده بر مجوز شیفت اولویت مطلق دارد** و شیفت در برنامه ماهانه به کاربر اختصاص می‌یابد.

### ساعات موظفی (بهره‌وری)

اگر `includedProductivityPlan = true`:
- سیستم سقف ساعات ماهانه را بر اساس آیین‌نامه ارتقای بهره‌وری بالینی و استاندارد کارگزینی بیمارستان محاسبه می‌کند، **مگر** `maxProductivityRequiredHours` مقدار مثبت داشته باشد (آن‌گاه همان عدد دستی اعمال می‌شود).
- **سطوح استاندارد موظفی ماه ۳۱ روزه (پایه ۱۷۶ ساعت):**
  - **سابقه کمتر از ۵ سال (طرحی / بدو خدمت):** کسر ۰ ساعت $\implies$ **۱۷۶ ساعت** موظفی.
  - **سابقه ۵ تا ۹ سال:** کسر ۱ ساعت هفتگی $\implies$ **۱۷۱ ساعت** موظفی.
  - **سابقه ۱۰ تا ۱۲ سال:** کسر ۲ ساعت هفتگی $\implies$ **۱۶۷ ساعت** موظفی.
  - **سابقه ۱۳ تا ۱۷ سال:** کسر ۳ ساعت هفتگی $\implies$ **۱۶۳ ساعت** موظفی.
  - **سابقه ۱۸ سال به بالا:**
    - شیفت ثابت روزکار یا دو نوبته: کسر ۳ ساعت هفتگی $\implies$ **۱۶۳ ساعت** موظفی.
    - شیفت در گردش سه نوبته: کسر ۴ ساعت هفتگی $\implies$ **۱۵۸ ساعت** موظفی.
- **اولویت پر کردن موظفی:** پرسنل غیرطرحی قبل از طرحی؛ پرسنل طرحی حداکثر تا موظفی (بدون اضافه‌کار).
- الگوریتم به‌صورت پیش‌فرض از **تجاوز از سقف موظفی** جلوگیری می‌کند (`EnforceProductivityHours = true` در کد).
- در خروجی شیفت‌بندی، ساعات کار شده و مازاد در بخش `statistics` قابل مشاهده است.

---

## ۱۰. مرحله ۸: تنظیمات زمان‌بندی دپارتمان

**یک رکورد برای هر بخش.** این تنظیمات در هر اجرای شیفت‌بندی خودکار خوانده می‌شوند.

**Endpoint:** `POST /api/DepartmentSchedulingSettings`

### ۱۰.۱. قوانین سخت (Hard Rules)

قوانین سخت: اگر فعال باشند، الگوریتم **هیچ انتسابی** که آن‌ها را نقض کند قبول نمی‌کند.

| فیلد | پیش‌فرض پیشنهادی | توضیح |
|------|------------------|--------|
| `forbidDuplicateDailyAssignments` | `true` | ممنوعیت تکرار همان لیبل در یک روز؛ صبح+عصر و صبح+شب مجازند |
| `enforceMaxShiftsPerDay` | `true` | اعمال سقف روزانه به عنوان قید سخت بر پایه تقویم هجری شمسی (۰۰:۰۰ تا ۲۳:۵۹) |
| `enforceMinRestDays` | `true` | حداقل فاصله استراحت بین دو شیفت |
| `enforceMaxConsecutiveShifts` | `true` | حداکثر شیفت پشت‌سرهم |
| `enforceWeeklyMaxShifts` | `false` | سقف هفتگی به‌صورت سخت؛ اگر false باشد **نرم** است |
| `enforceNightShiftMonthlyCap` | `false` | سقف شب ماهانه به‌صورت سخت؛ اگر false باشد **نرم** است |
| `enforceSpecialtyCapacity` | `true` | عدم تجاوز از ظرفیت تعریف‌شده در `ShiftRequiredSpecialty` |
| `allowCurrentMonthScheduling` | `false` | فقط تست: Optimize/حذف برای ماه جاری |
| `allowMonthlyRescheduleWithAutoDelete` | `false` | حذف خودکار برنامه قبلی هنگام Optimize مجدد |

**پیشنهاد بیمارستانی:**
- بخش‌های کوچک (زیر ۱۵ نفر): همه قوانین سخت را `true` بگذارید.
- بخش‌های با کمبود نیرو: `enforceWeeklyMaxShifts` و `enforceNightShiftMonthlyCap` را `false` بگذارید و با **وزن نرم** کنترل کنید.

### ۱۰.۲. مقادیر عددی قوانین

این مقادیر وقتی اعمال می‌شوند که قانون سخت مربوطه **فعال** باشد:

| فیلد | پیش‌فرض | پیشنهاد | توضیح |
|------|---------|---------|--------|
| `minRestDaysBetweenShifts` | `1` | `1` تا `2` | ۱ = حداقل یک روز کامل استراحت بین شیفت‌ها |
| `maxConsecutiveShifts` | `3` | `3` تا `5` | حداکثر شیفت متوالی |
| `maxShiftsPerWeek` | `5` | `4` تا `6` | سقف شیفت در هفته |
| `maxNightShiftsPerMonth` | `8` | `6` تا `10` | سقف شیفت شب در ماه |
| `maxShiftsPerDay` | `2` | `1` یا `2` | **سقف شیفت در یک روز شمسی:** اگر روی `1` تنظیم شود، هیچ پرسنلی مجاز به دریافت ۲ شیفت در یک روز شمسی نخواهد بود (حتی در صورت کمبود نیرو، شیفت دوم اضافه نمی‌شود و کسری ظرفیت گزارش می‌شود). اگر روی `2` باشد، ترکیب صبح+عصر یا صبح+شب در صورت نیاز مجاز است. |
| `maxConsecutiveNightShifts` | `2` | `2` | حداکثر شب پشت‌سرهم |

### ۱۰.۳. وزن قوانین نرم (Soft Weights)

قوانین نرم: نقض آن‌ها **جریمه** دارد، نه رد کامل. عدد بزرگ‌تر = اهمیت بیشتر.

| فیلد | پیش‌فرض | پیشنهاد کوچک | پیشنهاد متوسط/بزرگ | توضیح |
|------|---------|--------------|---------------------|--------|
| `genderBalanceWeight` | `1.0` | `0.5` | `1.0` | تعادل جنسیت (فقط وقتی در ظرفیت شیفت صریح تعریف شده) |
| `specialtyPreferenceWeight` | `1.0` | `0.5` | `1.0` | تطابق تخصص پرسنل با نیاز شیفت |
| `userUnwantedShiftWeight` | `1.0` | `1.0` | `1.5` | جریمه شیفت‌هایی که پرسنل نخواسته |
| `userPreferredShiftWeight` | `1.0` | `0.5` | `1.0` | پاداش شیفت‌های ترجیحی پرسنل |
| `weeklyMaxWeight` | `1.0` | `0.5` | `1.0` | جریمه نرم سقف هفتگی (وقتی سخت نیست) |
| `monthlyNightCapWeight` | `1.0` | `0.5` | `2.0` | جریمه نرم سقف شب ماهانه |
| `fairShiftCountBalanceWeight` | `1.0` | `0.3` | `0.8` | تعادل تعداد شیفت بین پرسنل |
| `extraShiftRotationWeight` | `1.0` | `0.5` | `1.0` | جلوگیری از دادن مکرر شیفت اضافه به یک نفر |
| `shiftLabelBalanceWeight` | `1.0` | `0.3` | `0.6` | تعادل صبح/عصر/شب برای پرسنل گردشی |
| `fairnessLookbackMonths` | `1` | `1` | `2` | چند ماه گذشته در محاسبه عدالت لحاظ شود |
| `overtimeDistributionWeight` | `1.0` | `0.5` | `1.0` | وزن اعمال ترجیح اضافه کار دپارتمان بر اساس سابقه |

### ۱۰.۳.۱. تنظیمات تمایل به اضافه کار و توزیع بر اساس سابقه

این تنظیمات مشخص می‌کند که شیفت‌های مازاد بر موظفی (اضافه کار) چگونه میان پرسنل توزیع شوند:

| فیلد | نوع | مقادیر مجاز | توضیح |
|------|:---:|:-----------:|-------|
| `enableOvertimeDistributionBySeniority` | `bool` | `true` / `false` | فعال‌سازی توزیع اضافه کار بر اساس سابقه (در صورت انتخاب نوع ۰ یا ۱، خودکار فعال می‌شود) |
| `overtimePreferenceType` | `int` | `0`, `1`, `2` | **۰:** علاقه‌مند به اضافه کار (سابقه بیشتر = اضافه کار بیشتر)<br/>**۱:** گریزان از اضافه کار (سابقه کمتر = اضافه کار بیشتر؛ حفاظت قوی از با‌سابقه‌ها)<br/>**۲:** خنثی (توزیع مساوی بین متقاضیان) |
| `overtimeDistributionWeight` | `double` | $\ge 0$ | وزن اثرگذاری در الگوریتم بهینه‌سازی |
| `overtimeSeniorityDistributionSlope` | `double` | `0.1` تا `10.0` | شیب صعودی/نزولی بودن توزیع اضافه کار (پیش‌فرض: `1.0`). شیب بالاتر اختلاف سهمیه اضافه کار را تشدید می‌کند |

> **نکات کلیدی:**
> ۱. **شیب توزیع اضافه کار (`overtimeSeniorityDistributionSlope`):** سوپروایزر با تعیین این عدد می‌تواند شیب صعودی یا نزولی بودن شیفت‌های مازاد را تنظیم کند؛ مقادیر ملایم (`0.5`) تفاوت‌های نرم ایجاد می‌کنند و مقادیر تندتر (`2.0` یا بالاتر) در حالت گریزان از اضافه کار، با‌سابقه‌ها را کاملاً به صفر اضافه کار نزدیک کرده و تمام بار اضافه کار را به کم‌سابقه‌ها محول می‌کنند.
> ۲. در دپارتمان‌های **گریزان از اضافه کار (نوع ۱)**، سیستم هوشمند با مهار اضافه کار پرسنل باسابقه و انتقال شیفت‌های مازاد به پرسنل کم‌سابقه، تضمین می‌کند که سابقه بیشتر منجر به ساعات اضافه کار به مراتب کمتری شود (حتی با احتساب کمتر بودن ساعات موظفی پرسنل باسابقه در قانون بهره‌وری).
> ۳. پرسنلی که در فرم کاربر فیلد **`overtimeConsent = false`** دارند، از دریافت اضافه کار معاف خواهند بود و این توزیع صرفاً میان پرسنل متقاضی (`overtimeConsent = true`) صورت می‌گیرد.

### ۱۰.۴. نمونه تنظیمات آماده

**بخش عمومی ۲۰ نفره (پیشنهاد شروع):**

```json
{
  "departmentId": 1,
  "forbidDuplicateDailyAssignments": true,
  "enforceMaxShiftsPerDay": true,
  "enforceMinRestDays": true,
  "enforceMaxConsecutiveShifts": true,
  "enforceWeeklyMaxShifts": false,
  "enforceNightShiftMonthlyCap": false,
  "enforceSpecialtyCapacity": true,
  "minRestDaysBetweenShifts": 1,
  "maxConsecutiveShifts": 3,
  "maxShiftsPerWeek": 5,
  "maxNightShiftsPerMonth": 8,
  "maxShiftsPerDay": 1,
  "genderBalanceWeight": 1.0,
  "userUnwantedShiftWeight": 1.5,
  "userPreferredShiftWeight": 1.0,
  "fairShiftCountBalanceWeight": 0.5,
  "extraShiftRotationWeight": 0.8,
  "shiftLabelBalanceWeight": 0.6,
  "fairnessLookbackMonths": 2
}
```

**بخش با کمبود نیرو (انعطاف بیشتر):**

```json
{
  "enforceWeeklyMaxShifts": false,
  "enforceNightShiftMonthlyCap": false,
  "enforceSpecialtyCapacity": true,
  "weeklyMaxWeight": 0.3,
  "monthlyNightCapWeight": 0.5,
  "fairShiftCountBalanceWeight": 0.8
}
```

> **یادداشت فنی:** برخی فیلدهای موجود در جدول تنظیمات (مثل حداقل شیفت پرسنل گردشی، الزام مسئول شیفت، تنظیمات شب‌دوست/شب‌گریز پیشرفته) در پایگاه ذخیره می‌شوند اما هنوز در موتور الگوریتم SA به‌طور کامل اعمال نشده‌اند. فیلدهای اصلی جدول بالا در حال حاضر فعال هستند.

---

## ۱۱. مرحله ۹: تنظیمات الگوریتم

پارامترهای الگوریتم از جدول `AlgorithmSettings` خوانده می‌شوند؛ **در بدنه درخواست شیفت‌بندی ارسال نمی‌شوند.**

**Endpoint:** `POST /api/AlgorithmSettings`

### ۱۱.۱. انتخاب الگوریتم

| `algorithmType` | نام | کاربرد |
|-----------------|-----|--------|
| `1` | Simulated Annealing (SA) | **پیشنهاد اصلی** — سریع، مناسب اکثر بخش‌ها |
| `2` | OR-Tools CP-SAT | دقت بالاتر، زمان بیشتر |
| `3` | Hybrid | ترکیب SA و OR-Tools برای مسائل پیچیده |

```json
{
  "departmentId": 1,
  "algorithmType": 1
}
```

- `departmentId = null` → تنظیمات **سراسری** (برای همه بخش‌ها)
- اگر برای بخش خاص تنظیم شود، **اولویت با تنظیمات همان بخش** است.

---

### ۱۱.۲. پارامترهای Simulated Annealing — توضیح کامل

الگوریتم SA با شبیه‌سازی **فرآیند آنیلینگ فلز** کار می‌کند: در ابتدا با دمای بالا تغییرات بزرگ (حتی بد) را می‌پذیرد و به‌تدریج دما پایین می‌آید تا به یک برنامه پایدار برسد.

| پارامتر | پیش‌فرض سیستم | توضیح |
|---------|---------------|--------|
| `SA_InitialTemperature` | `1000` | دمای اولیه — هرچه بالاتر، جست‌وجوی گسترده‌تر در ابتدا |
| `SA_FinalTemperature` | `0.1` | دمای پایانی — وقتی دما به این مقدار برسد، جست‌وجو متوقف می‌شود |
| `SA_CoolingRate` | `0.95` | نرخ کاهش دما در هر تکرار (ضرب در دمای فعلی) |
| `SA_MaxIterations` | `10000` | حداکثر تعداد تکرار کل |
| `SA_MaxIterationsWithoutImprovement` | `1000` | اگر این تعداد تکرار بدون بهبود بگذرد، زودتر متوقف می‌شود |

#### `SA_InitialTemperature` — دمای اولیه

**چیست؟** میزان «شجاعت» الگوریتم در پذیرش راه‌حل‌های بدتر برای فرار از بهینه محلی.

| مقدار | اثر | کی استفاده کنیم |
|-------|-----|-----------------|
| `300` – `500` | جست‌وجوی محدود، سریع | بخش کوچک (زیر ۱۰ نفر، زیر ۱۴ روز) |
| `800` – `1200` | متعادل | **پیشنهاد عمومی** |
| `1500` – `3000` | جست‌وجوی وسیع | بخش بزرگ یا قوانین زیاد |

#### `SA_FinalTemperature` — دمای پایانی

**چیست؟** آستانه توقف؛ وقتی دما خیلی پایین آید، فقط بهبودهای واضح پذیرفته می‌شود.

| مقدار | پیشنهاد |
|-------|---------|
| `0.1` | پیش‌فرض — مناسب اکثر موارد |
| `0.01` | دقت بیشتر، زمان بیشتر |
| `0.5` | سریع‌تر، کیفیت کمتر |

#### `SA_CoolingRate` — نرخ سرمایش

**چیست؟** سرعت کاهش دما. مقدار نزدیک به ۱ = سرمایش آهسته‌تر = جست‌وجوی عمیق‌تر.

| مقدار | اثر |
|-------|-----|
| `0.90` – `0.93` | سریع‌تر، برای مسائل کوچک |
| `0.94` – `0.96` | **پیشنهاد عمومی** |
| `0.97` – `0.99` | آهسته، برای مسائل بزرگ و پیچیده |

> باید بین `0.1` و `0.99` باشد. دمای اولیه باید **بزرگ‌تر** از دمای پایانی باشد.

#### `SA_MaxIterations` — حداکثر تکرار

**چیست؟** سقف تعداد حلقه‌های بهینه‌سازی.

| اندازه مسئله | پیشنهاد |
|--------------|---------|
| کوچک (≤۱۰ نفر، ≤۱۴ روز) | `2000` – `4000` |
| متوسط (۱۰–۳۰ نفر، ۱۵–۳۱ روز) | `6000` – `10000` |
| بزرگ (۳۰+ نفر، ۳۲–۶۲ روز) | `10000` – `15000` |

**اجرا در پس‌زمینه (async):** سیستم حداکثر را به `4000` تکرار محدود می‌کند.

#### `SA_MaxIterationsWithoutImprovement` — توقف زودهنگام

**چیست؟** اگر الگوریتم مدتی بهبود نیافت، بی‌فایده ادامه نمی‌دهد.

| پیشنهاد | مقدار |
|---------|-------|
| مسائل کوچک | `200` – `400` |
| عمومی | `600` – `1000` |
| مسائل سخت | `1200` – `2000` |

**پس‌زمینه:** حداکثر `800`.

---

### ۱۱.۳. پروفایل‌های آماده SA

#### پروفایل سریع (بخش کوچک، تست اولیه)

```json
{
  "departmentId": 1,
  "algorithmType": 1,
  "SA_InitialTemperature": 500,
  "SA_FinalTemperature": 0.1,
  "SA_CoolingRate": 0.93,
  "SA_MaxIterations": 3000,
  "SA_MaxIterationsWithoutImprovement": 400
}
```

زمان تقریبی: چند ثانیه تا ۳۰ ثانیه.

#### پروفایل استاندارد (پیشنهاد تولید)

```json
{
  "departmentId": 1,
  "algorithmType": 1,
  "SA_InitialTemperature": 1000,
  "SA_FinalTemperature": 0.1,
  "SA_CoolingRate": 0.95,
  "SA_MaxIterations": 10000,
  "SA_MaxIterationsWithoutImprovement": 1000
}
```

زمان تقریبی: ۳۰ ثانیه تا ۲ دقیقه.

#### پروفایل دقیق (بخش بزرگ یا قوانین سخت زیاد)

```json
{
  "departmentId": 1,
  "algorithmType": 1,
  "SA_InitialTemperature": 1500,
  "SA_FinalTemperature": 0.05,
  "SA_CoolingRate": 0.97,
  "SA_MaxIterations": 15000,
  "SA_MaxIterationsWithoutImprovement": 1500
}
```

زمان تقریبی: ۲ تا ۵ دقیقه.

---

### ۱۱.۴. پارامترهای OR-Tools (الگوریتم ۲)

اگر از OR-Tools استفاده می‌کنید:

| پارامتر | پیش‌فرض | پیشنهاد | توضیح |
|---------|---------|---------|--------|
| `ORT_MaxTimeInSeconds` | `300` | `60` – `180` | حداکثر زمان حل (ثانیه) |
| `ORT_NumSearchWorkers` | `4` | `2` | تعداد رشته جست‌وجو |
| `ORT_RelativeGapLimit` | `0.01` | `0.01` – `0.05` | فاصله از بهینه (۱٪ = تقریباً بهینه) |
| `ORT_LogSearchProgress` | `false` | `false` | لاگ برای دیباگ |

> درخواست **همزمان (sync)** روی سرور ابری (مثل Liara) معمولاً بعد از ۶۰ ثانیه قطع می‌شود. OR-Tools را با **async** اجرا کنید.

---

### ۱۱.۵. پارامترهای Hybrid (الگوریتم ۳)

| پارامتر | پیش‌فرض | توضیح |
|---------|---------|--------|
| `HYB_Strategy` | `1` | `1`=OR-Tools اول، `2`=SA اول، `3`=موازی |
| `HYB_MaxIterations` | `5` | تعداد تکرار ترکیبی |
| `HYB_ComplexityThreshold` | `100` | آستانه پیچیدگی برای انتخاب استراتژی |

**پیشنهاد:** برای اکثر بخش‌ها ابتدا SA (`algorithmType=1`) را امتحان کنید. Hybrid فقط وقتی SA یا OR-Tools به‌تنهایی کافی نیست.

---

## ۱۲. مرحله ۱۰: درخواست‌های شیفت پرسنل

پرسنل می‌توانند مرخصی (عدم‌حضور کل‌روز) یا درخواست حضور در یک شیفت مشخص ثبت کنند.

**جریان پیشنهادی:**
1. سوپروایزر: تنظیم **سهمیه شب ماهانه** برای ماه موردنظر
2. پرسنل: `POST /api/ShiftRequest` — ثبت درخواست
3. سوپروایزر: تأیید یا رد درخواست
4. **تا وقتی درخواست Pending در بازه وجود دارد، شیفت‌بندی مسدود است.**

### قواعد مهم درخواست و اولویت‌های مطلق

| موضوع | قاعده |
|--------|--------|
| حضور کل‌روز | **مجاز نیست** (`requestType=0` فقط برای مرخصی/`requestAction=1`) |
| حضور | باید `requestType=1` + `shiftLabel` (۰ صبح / ۱ عصر / ۲ شب) |
| OFF صبح یا کل‌روز (تأییدشده) | **شب روز قبل** به کاربر داده نمی‌شود |
| تأیید حضور | سوپروایزر نمی‌تواند بیش از **ظرفیت شیفت/تخصص** همان روز تأیید کند (`ApprovedOnShiftCapacityValidator`) |
| **تأیید مرخصی (سقف مجاز روزانه)** | سوپروایزر نمی‌تواند بیش از **حداکثر مرخصی مجاز روزانه** برای یک تخصص در یک روز مرخصی تأیید کند (`ApprovedLeaveCapacityValidator`). در صورت تکمیل سقف، پیام خطای شفاف به همراه تاریخ، آمار پرسنل و اسامی افراد دارای مرخصی نمایش داده شده و عملیات متوقف می‌شود. |
| درخواست شب | فقط اگر سهمیه ماهانه باشد و از سهمیه تجاوز نشود |
| ترکیب روزانه | در حالت `maxShiftsPerDay = 1`: **فقط ۱ شیفت در روز**؛ در حالت `maxShiftsPerDay = 2`: مجاز: صبح+عصر، صبح+شب — ممنوع: عصر+شب، شب→صبح روز بعد |
| **اولویت بر تعطیلات رسمی (پرسنل فیکس)** | پرسنل دارای شیفت ثابت (مثلاً فیکس صبح) در تعطیلات رسمی به‌صورت پیش‌فرض OFF هستند؛ اما اگر **درخواست شیفت تأییدشده‌ای** برای روز تعطیل داشته باشند، درخواست بر تعطیلی اولویت داشته و شیفت به کاربر تخصیص می‌یابد. |
| **اولویت بر مجوز شیفت** | اگر کاربر مجاز به شیفتی نباشد (مثلاً کاربر گردشی غیرمجاز برای شیفت صبح) اما **درخواست تأییدشده** برای آن شیفت داشته باشد، درخواست تأییدشده بر مجوز شیفت اولویت دارد. |
| **اولویت بر قانون تناوب هفتگی شیفت‌های روزانه** | اگر برای کاربری قانون تناوب هفتگی شیفت‌های روزانه فعال باشد (مثلاً در هفته دوم فقط شیفت عصر داشته باشد)، در صورت وجود **درخواست شیفت تأییدشده** برای شیفت متضاد (مثلاً صبح)، درخواست تأییدشده از قاعده تناوب مستثنا بوده و شیفت صبح در برنامه اعمال می‌شود. |

#### فرمول محاسبه حداکثر مرخصی مجاز روزانه:
$$\text{حداقل پرسنل مورد نیاز امروز} = \text{مجموع نیاز شیفت‌های امروز} + \text{نیاز شیفت شب روز قبل (استراحت اجباری)}$$
$$\text{حداکثر مرخصی مجاز روزانه} = \max\Big(0, \text{کل پرسنل فعال تخصص} - \text{حداقل پرسنل مورد نیاز امروز}\Big)$$

- **مثال:** در بخشی با ۲۱ پرستار فعال، مجموع نیاز شیفت‌های امروز ۱۱ نفر (صبح: ۴، عصر: ۳، شب: ۴) و نیاز شیفت شب روز قبل ۴ نفر باشد، حداقل ۱۵ نفر نیرو الزامی است و حداکثر سقف مرخصی همزمان برابر با $21 - 15 = \mathbf{6 \text{ نفر}}$ خواهد بود. تأیید نفر هفتم منجر به مسدودسازی و خطای سیستم می‌گردد.

درخواست‌های **تأییدشده** به‌صورت خودکار در الگوریتم اعمال می‌شوند:
- عدم‌حضور → پرسنل در آن روز/شیفت انتساب نمی‌گیرد؛ OFF صبح/کل‌روز → شب روز قبل هم مسدود می‌شود
- حضور شیفت‌مشخص → انتساب اجباری همان شیفت (دارای اولویت بر مجوزهای شیفت، تناوب هفتگی روزانه و تعطیلات رسمی پرسنل فیکس)
- سهمیه شب ماهانه → حداقل تعداد شب اجباری در Optimize. تا زمانی که مجموع سهمیه‌های معین شب پرسنل از کل تقاضای شیفت شب بخش بیشتر نباشد، جابجایی سهمیه‌ها میان پرسنل بدون خطای کسری، از طریق الگوریتم چرخش زنجیره‌ای چندمرحله‌ای تضمین می‌شود و پرسنل بدون سهمیه شب ظرفیت شب را اشغال نمی‌کنند.

> **نکته قانون تناوب هفتگی شیفت‌های روزانه:**
> در سهمیه ماهانه روزانه (`UserMonthlyDayShiftQuota`)، سوپروایزر می‌تواند فیلد `isWeeklyAlternatingActive = true` را برای پرسنل فعال کرده و شیفت هفته اول ماه شمسی (`firstWeekShiftLabel` برابر ۰ برای صبح یا ۱ برای عصر) را تعیین کند. در این صورت شیفت‌های روزانه پرسنل بر اساس هفته‌های تقویمی استاندارد شمسی (شنبه تا جمعه) به صورت متناوب (یک هفته صبح / یک هفته عصر) تخصیص می‌یابد. **شیفت‌های شب، آف و مرخصی‌ها کاملاً از این قاعده مستثنی هستند** و طبق روال عادی خود تخصیص می‌یابند. همچنین هرگونه **درخواست شیفت تأییدشده** برای شیفت متضاد بر این قاعده تقدم داشته و بدون مانع در برنامه اعمال خواهد شد.

---

## ۱۲.۵ حذف و ایجاد مجدد برنامه ماهانه

- `POST /DeleteMonthlySchedule` با `departmentId` + `persianYear` + `persianMonth`
- فقط قبل از شروع ماه (مگر `allowCurrentMonthScheduling`)
- با `allowMonthlyRescheduleWithAutoDelete` برنامه قبلی هنگام Optimize جدید خودکار جایگزین می‌شود

---

## ۱۳. مرحله ۱۱: اجرای شیفت‌بندی

### گام ۱: اعتبارسنجی (توصیه می‌شود)

```http
POST /validate
Content-Type: application/json
Authorization: Bearer {token}
```

```json
{
  "departmentId": 1,
  "startDate": "1404/07/01",
  "endDate": "1404/07/31",
  "algorithm": 1
}
```

اگر خطا برگرداند، قبل از optimize رفع کنید.

### گام ۲: بهینه‌سازی (بدون ذخیره)

```http
POST /optimize
```

همان بدنه بالا. خروجی را بررسی کنید (`finalScore`, `violations`, `assignments`).

### گام ۳: ذخیره برنامه

**روش یک‌مرحله‌ای (sync):**

```http
POST /optimize-and-save
```

**روش پس‌زمینه (برای مسائل سنگین یا OR-Tools/Hybrid):**

```http
POST /optimize-and-save-async
```

پاسخ:

```json
{
  "jobId": "abc-123",
  "statusUrl": "scheduling-jobs/abc-123"
}
```

**پیگیری وضعیت:**

```http
GET /scheduling-jobs/{jobId}
```

وضعیت‌ها: `Queued` → `Running` → `Completed` یا `Failed`

### انتخاب روش اجرا

| الگوریتم | روش پیشنهادی | دلیل |
|---------|--------------|------|
| SA (`1`) | `optimize-and-save` | معمولاً زیر ۶۰ ثانیه |
| OR-Tools (`2`) | `optimize-and-save-async` | زمان‌بر، احتمال timeout در sync |
| Hybrid (`3`) | `optimize-and-save-async` | ترکیبی و سنگین |

---

## ۱۴. تفسیر خروجی و عیب‌یابی

### فیلدهای مهم پاسخ

| فیلد | معنی |
|------|------|
| `finalScore` | امتیاز نهایی — **کمتر = بهتر** |
| `violations` | لیست نقض قوانین نرم |
| `assignments` | لیست انتساب‌ها (`userId`, `shiftId`, `date`, `isOnCall`) |
| `algorithmUsed` | الگوریتم واقعی استفاده‌شده |
| `statistics` | آمار شیفت‌ها، ساعات موظفی، توزیع |

### خطاهای رایج

| خطا | علت محتمل | راه‌حل |
|-----|-----------|--------|
| درخواست‌های در انتظار | `ShiftRequest` تأیید نشده | تأیید/رد درخواست‌ها |
| پرسنل فعال نیست | `isActive=false` | فعال‌سازی کاربران |
| ظرفیت پر نشده | کمبود نیرو یا قوانین سخت زیاد | کاهش قوانین سخت یا افزودن پرسنل |
| ترکیب جنسیت اشتباه | `requiredMaleCount`/`requiredFemaleCount` | ظرفیت شیفت و جنسیت پرسنل را بررسی کنید |
| آنکال خالی | `onCallTottalCount` بدون پرسنل کافی | پرسنل یا ظرفیت آنکال را اصلاح کنید |
| سهمیه حداقل شیفت شب رعایت نشد | مجموع سهمیه‌ها بیشتر از ظرفیت ماه است، یا سهمیه‌ها متناسب نیست | مجموع سهمیه شب پرسنل نباید از کل ظرفیت شب بخش (تعداد شب‌ها × نیاز هر شب) بیشتر باشد؛ سیستم تضمین می‌کند با سهمیه معتبر، تمام سهمیه‌ها بدون نقص رعایت شوند |
| timeout / 502 | الگوریتم سنگین در sync | از `optimize-and-save-async` استفاده کنید |
| سقف موظفی | `includedProductivityPlan=true` | ساعات موظفی یا بازه را تنظیم کنید |

### چک‌لیست قبل از اجرا

- [ ] تقویم سال مورد نظر seed شده
- [ ] بخش، تخصص، شیفت و ظرفیت تخصصی تعریف شده
- [ ] حداقل به اندازه نیاز روزانه، پرسنل فعال با تخصص و جنسیت مناسب وجود دارد
- [ ] تنظیمات `DepartmentSchedulingSettings` برای بخش ثبت شده
- [ ] (اختیاری) تنظیمات `AlgorithmSettings` با `algorithmType=1` ثبت شده
- [ ] درخواست‌های شیفت در بازه بررسی شده‌اند
- [ ] `validate` بدون خطای بلوکه‌کننده اجرا شده

---

## ۱۵. پیشنهاد تنظیمات بر اساس اندازه بخش

### بخش کوچک (۵–۱۵ نفر، ۱ تخصص اصلی)

| مورد | پیشنهاد |
|------|---------|
| الگوریتم | SA (`1`) |
| بازه | ۱۴–۳۱ روز |
| SA | پروفایل سریع |
| قوانین سخت | همه فعال |
| وزن عدالت | `0.3` – `0.5` |

### بخش متوسط (۱۵–۴۰ نفر، ۲–۳ تخصص)

| مورد | پیشنهاد |
|------|---------|
| الگوریتم | SA (`1`) |
| بازه | ۲۸–۳۱ روز |
| SA | پروفایل استاندارد |
| `enforceWeeklyMaxShifts` | `false` (نرم) |
| `fairShiftCountBalanceWeight` | `0.8` |

### بخش بزرگ (۴۰+ نفر، چند تخصص، ۳ شیفت)

| مورد | پیشنهاد |
|------|---------|
| الگوریتم | SA یا Hybrid |
| بازه | ۳۱ روز (تقسیم ماه به دو بار در صورت نیاز) |
| SA | پروفایل دقیق |
| اجرا | `optimize-and-save-async` |
| `fairnessLookbackMonths` | `2` |

---

## پیوست: حرکت‌های الگوریتم SA (برای درک بهتر)

الگوریتم SA در هر تکرار یکی از این تغییرات را روی برنامه فعلی اعمال می‌کند:

| حرکت | توضیح |
|------|--------|
| **انتساب مجدد** | جایگزینی یک پرسنل با پرسنل دیگر همان تخصص |
| **جابجایی** | تعویض دو پرسنل در یک شیفت/روز/تخصص |
| **افزودن** | پر کردن جای خالی (کم‌کاری) |
| **حذف** | حذف نیروی اضافه (اضافه‌کاری نسبت به ظرفیت) |

امتیازدهی:
- **جریمه سنگین** برای جای خالی بودن شیفت
- **جریمه** برای نقض قوانین نرم (ترجیحات، عدالت، سقف شب)
- **رد کامل** برای نقض قوانین سخت

---

## خلاصه مسیر API

| مرحله | Endpoint |
|--------|----------|
| تقویم | `POST /api/CalendarSeeder/SeedYear` |
| بیمارستان | `POST /api/Hospital` |
| دپارتمان | `POST /api/Department` |
| تخصص | `POST /api/Specialty` |
| شیفت | `POST /api/Shift` |
| ظرفیت | `POST /api/ShiftRequiredSpecialty` |
| کاربر | `POST /api/User` |
| تنظیمات بخش | `POST /api/DepartmentSchedulingSettings` |
| تنظیمات الگوریتم | `POST /api/AlgorithmSettings` |
| اعتبارسنجی | `POST /validate` |
| شیفت‌بندی | `POST /optimize-and-save` یا `POST /optimize-and-save-async` |
| وضعیت job | `GET /scheduling-jobs/{jobId}` |

---

*آخرین به‌روزرسانی: بر اساس نسخه فعلی کد ShiftYar — الگوریتم Simulated Annealing با پشتیبانی از قوانین جنسیت صریح، آنکال، و بهره‌وری.*
